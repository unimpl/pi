# 阅读界面

模式：Read。用户确认的方向为浅暖纸面、深色正文、克制强调色、工程图与长文结合。

正文用本地中文字体栈，标题用中文宋体栈形成书籍感；代码使用系统等宽字体。无远程字体依赖。左侧章节导航、中央正文、右侧页内目录；小屏收起章节。强调色墨绿用于当前路径，赭色标记边界和推断。章节数字表示阅读顺序。

图解只有数据流、时序和状态转换，不添加装饰插画。全部图表由 Mermaid 源文件经 beautiful-mermaid 预渲染；不使用手写 SVG 图表或动画。核心事实在静态正文中完整表达。

交互只保留本地全文搜索、移动目录和源码折叠。没有阅读进度、教学动画、图表下载、账号、网络依赖或模型调用。每章先展示设计动机、行业常见方案、实现重点和可忽略项，再进入 Pi 的实现与证据。
