window.STUDY_SEARCH = [{"title": "01 先认识 Pi 的核心对象 / 第一层：当前活动对象及其数量", "url": "foundations.html#objects", "text": "    Pi 对象  数量关系  它拥有或负责什么       AgentSessionRuntime   CLI 通常有 1 个当前槽位  运行时容器，不是对话本身。持有当前 AgentSession 及与工作目录绑定的服务；切换会话时用新对象替换旧对象。     AgentSession   当前槽位中 1 个  应用协调层。持有 1 个 Agent、1 个 SessionManager 和设置、资源，把扩展、压缩、重试、模型切换等产品行为组织成统一 API。     SessionManager   每个 AgentSession 1 个  管理当前持久会话树；保存消息、模型切换、压缩摘要和扩展数据。     Agent   每个 AgentSession 1 个  执行内核。持有当前模型、工具、消息、队列、取消控制器和运行状态。     如果写成 Go， *Agent  表示“指向一个 Agent”，不是多个 Agent。多个 Agent 通常会写成  []*Agent  或  map[ID]*Agent ；当前 AgentSession 的字段是单个  agent: Agent ，没有 Agent 数组。  “协调层”不是 UI，也不是空包装。界面把  prompt 、 abort 、 compact 、模型切换等操作交给 AgentSession；AgentSession 再调用 Agent，监听执行事件，通过 SessionManager 持久化，并运行扩展与恢复策略。这样，低层 Agent 不需要知道 CLI、会话文件或产品设置。   与行业术语的近似映射： 可以先把 Pi 的  Agent  理解成 harness 的执行核心，把  AgentSession  理解成可供 CLI 使用的 agent 应用层，把整个 Pi CLI 理解成用户实际使用的 coding agent 产品。这个映射只是入门近似：行业里的 harness 往往还包括模型适配、上下文治理、权限与可观测性；Pi 将其中一些职责放在  AgentSession  或更外层。行业里的 “agent” 也没有统一的数据结构边界。  判断边界时不要只看类名，要看职责： Agent  回答“下一次模型调用和工具循环怎样继续”， AgentSession  回答“这个产品会话怎样保存、配置、扩展、压缩和恢复”。   Runtime 不是另一个 session： 它更像 Go 中的  struct { current *AgentSession; services Services; factory Factory } 。 current  才是用户当前操作的 AgentSession；Runtime 保存这个引用，并提供  newSession 、 switchSession 、 fork  和  dispose  等生命周期操作。  为什么还要这一层？切换会话不只是换一份聊天记录。Pi 必须先终止当前执行，发送关闭事件，释放旧扩展，再根据目标会话的工作目录重建相关服务，最后把界面重新绑定到新 AgentSession。AgentSessionRuntime 把这套替换顺序集中起来，避免 CLI 各处分别实现。  启动一次 Pi CLI，通常只有一个当前活动的 AgentSession。磁盘上可以存在很多历史 session 文件，但它们不是同时塞在这个 AgentSession 里的多个 Agent。用户切换或恢复会话时，AgentSessionRuntime 会拆除当前运行对象，再创建并绑定新的 AgentSession。仓库的新 Harness API 属于另一条更通用、仍在演进的路径，第一遍不混入这里。 "}, {"title": "01 先认识 Pi 的核心对象 / 第二层：run 与 turn 是时间边界", "url": "foundations.html#run-turn", "text": "  run  不是长期对象，而是 Agent 对一次  prompt()  或  continue()  的执行生命周期。从  agent_start  开始，到  agent_end  事件的监听器全部完成后，Agent 才重新空闲。同一个 Agent 不允许并发启动两个 run；运行中追加输入要进入 steering 或 follow-up 队列。   turn  可以先理解为“一次完整的模型请求与响应周期”，但它还包括该 assistant 响应触发的全部工具调用和工具结果。它从  turn_start  开始：把待处理消息放入上下文，请求一次模型并得到一条完整 assistant message；若其中有 toolCall，则执行工具并生成 toolResult；最后发送  turn_end 。所以 turn 不是“一个 user message 加一个 assistant message”的对话对子。  如果第一个 turn 调用了 read，工具结果被追加进消息后，循环还要再次请求模型，让模型基于读取结果继续回答，于是同一个 run 进入第二个 turn。  因此关系是：一个 AgentSession 长期持有一个 Agent；Agent 在某个时刻最多运行一个 run；一个 run 包含一个或多个 turn。不要把“用户发了一条消息”“调用了一次模型”“完成了一次任务”都叫一轮。 "}, {"title": "01 先认识 Pi 的核心对象 / 把实际使用过程映射到对象", "url": "foundations.html#usage-trace", "text": "   启动 Pi：  createAgentSession()  创建当前活动的 AgentSession，并装配 Agent 与 SessionManager。默认是新会话；使用恢复、指定 session 等入口时，也可能加载已有记录。   Pi 空闲时发送普通消息： 文本先变成一条 role 为 user 的 AgentMessage，然后  Agent.prompt()  启动一次 run。   模型直接回答： 运行时请求模型一次，得到一条不含工具调用的 assistant message。这次请求与响应构成一个 turn，当前 run 随后结算。   模型要求读取文件： 第一个 turn 请求模型并得到含 read toolCall 的 assistant message，随后执行 read 并追加 toolResult；为了让模型看到结果，同一个 run 再进入第二个 turn并再次请求模型。它可以继续产生更多 turn。   run 结束后再次输入： 新的普通消息通常启动新的 run，但仍在同一个 AgentSession 和持久 session 中，因此会接着已有记录工作。    run\n├─ turn 1：请求模型 → assistant(toolCall: read) → 执行 read → toolResult\n└─ turn 2：携带 toolResult 请求模型 → assistant(最终文字回答)   两个例外需要记住。第一，运行中输入的 steering 或 follow-up 会排队进入当前活动 run，不会并发启动另一个 run。第二，斜杠命令、本地 shell 等产品操作不一定经过模型，所以“界面里提交一次输入”不总是等于一次 Agent run。 "}, {"title": "01 先认识 Pi 的核心对象 / 第三层：Message 是各层之间的共同记录", "url": "foundations.html#messages", "text": "  AgentState.messages  是当前 transcript。基础角色包括 system、user、assistant 和 toolResult；coding-agent 还能增加只供应用使用的自定义消息，随后在模型请求边界转换或过滤。  以“解释 README”为例：system 保存规则和工具声明；user 保存目标；assistant 先返回一个 read toolCall；toolResult 带相同的 toolCallId 保存文件内容；第二个 assistant 才生成解释。assistant 消息因此不等于最终答案，它也可以是一次待执行的动作请求。  SessionManager 保存的范围更大：除了 message 条目，还有模型变更、thinking level、压缩摘要、分支摘要和扩展数据。模型真正看到的上下文只是从这些记录构建出的投影，不是整个 SessionManager。   session 文件不等于 AgentSession： AgentSession 是内存中的运行对象；JSONL 文件只是 SessionManager 管理的持久记录。恢复时，Pi 读取文件、构建当前分支的模型上下文，再创建新的 Agent 和 AgentSession。文件没有保存工具函数、事件监听器、取消控制器或正在流式生成的半条消息。 "}, {"title": "01 先认识 Pi 的核心对象 / 第四层：Tool 定义能力，toolCall 与 toolResult 记录一次使用", "url": "foundations.html#tools", "text": "  AgentTool  是长期注册在 Agent 上的能力定义，包含名称、描述、参数 schema 和  execute() 。它类似 Go 中带元数据的接口实现，或 Rust 中实现 trait 的值。   toolCall  是某个 assistant 消息中的一次调用请求； toolResult  是执行后的消息。二者通过 toolCallId 关联。一次 assistant 响应可以对同一个工具发出多个调用，因此不能只用工具名配对。  toolCall ID 通常由模型服务的响应协议提供，Pi 的 provider 适配层读取后放进  toolCall.id 。运行时执行工具后，把同一个值复制到  toolResult.toolCallId 。它应被当作不透明的关联键，而不是带业务含义的 ID；某些 provider 不提供或重复提供 ID 时，Pi 的适配层会补生成唯一值。  模型只能产生 toolCall 数据。真正的文件读取发生在运行时查找工具、校验参数、通过策略钩子并调用 execute 之后。这个分离让宿主可以拒绝、取消、记录或替换副作用。 "}, {"title": "01 先认识 Pi 的核心对象 / 第五层：Model 与 StreamFn 位于 Agent 的外部边界", "url": "foundations.html#model", "text": "  Model  描述要调用的具体模型及其 provider、API、上下文窗口等能力； StreamFn  是 Agent loop 请求一次模型响应的函数契约。Agent 不直接依赖某家供应商客户端。  一次 turn 调用 StreamFn，消费响应事件并结算出完整 assistant 消息。流式事件只让界面逐步看到文本或工具参数；是否继续下一 turn，仍由完整响应、工具结果和队列状态共同决定。  对第一遍阅读而言，记住调用方向即可：AgentSession 组织产品策略；Agent 驱动 run；run 产生 turn；turn 通过 StreamFn 请求 Model，并通过 AgentTool 接触外部环境；产生的消息同时更新 Agent 状态并由 SessionManager 持久保存。 "}, {"title": "01 先认识 Pi 的核心对象 / 先掌握关系，再读实现细节", "url": "foundations.html#priority", "text": " 第一遍必须能回答五个问题：谁保存完整会话；谁持有当前执行状态；一次 run 何时开始和真正结束；为什么一个 run 会有多个 turn；toolCall 为什么不是工具已经执行。   查看参考答案    谁保存完整会话？   SessionManager  管理完整的持久会话树，并在启用持久化时写入 JSONL。 AgentState.messages  只保存当前 Agent 使用的 transcript，不代表包含所有分支和会话元数据。   谁持有当前执行状态？   Agent  持有  AgentState  及当前 active run，包括模型、工具、消息、流式中的 assistant message、待完成工具调用、队列和取消信号。AgentSession 负责协调产品策略，不直接实现底层循环。   一次 run 何时开始和真正结束？  调用空闲 Agent 的  prompt()  或  continue()  后，Agent 先创建 active run 与 AbortController、设置  isStreaming ，循环再发送  agent_start 。 agent_end  表示循环不再产生事件；只有它的异步监听器全部完成， finishRun()  清除 active run，并使  waitForIdle()  完成后，run 才真正结算。   为什么一个 run 会有多个 turn？  一个 turn 只包含一次 assistant 响应及其工具结果。若响应要求调用工具，模型此时还没看到结果；运行时必须把 toolResult 加入上下文，再发起下一次模型请求，因此同一个 run 会进入下一个 turn。steering 和 follow-up 也可能让该 run 继续产生 turn。   为什么 toolCall 不代表工具已经执行？  toolCall 只是 provider 返回的结构化请求数据。运行时仍需查找工具、准备和校验参数、经过策略钩子，再调用  execute() 。执行完成或失败后产生独立的 toolResult；只有 toolResult 才记录宿主实际得到的结果。    现在可以忽略事件的全部变体、压缩算法、扩展钩子和多进程协议。它们都是在上述对象关系成立之后，为产品需求增加的机制。下一章再把这些对象放回包和模块边界中。 "}, {"title": "先认识 Pi 的核心对象 / 启动路径装配 AgentSession、Agent 与 SessionManager。", "url": "foundations.html#evidence", "text": "  启动路径装配 AgentSession、Agent 与 SessionManager。  packages/coding-agent/src/core/sdk.ts · L388    388  \tconst session = new AgentSession({\n 389  \t\tagent,\n 390  \t\tsessionManager,\n 391  \t\tsettingsManager,\n 392  \t\tcwd,\n 393  \t\tscopedModels: options.scopedModels,\n 394  \t\tresourceLoader,\n 395  \t\tcustomTools: options.customTools,\n 396  \t\tmodelRuntime,\n 397  \t\tinitialActiveToolNames,\n 398  \t\tallowedToolNames,\n 399  \t\texcludedToolNames,\n 400  \t\textensionRunnerRef,\n 401  \t\tsessionStartEvent: options.sessionStartEvent,\n 402  \t});\n 403  \tconst extensionsResult = resourceLoader.getExtensions();   查看本地源码快照与行号  "}, {"title": "先认识 Pi 的核心对象 / AgentSession 同时持有 Agent 与 SessionManager。", "url": "foundations.html#evidence", "text": "  AgentSession 同时持有 Agent 与 SessionManager。  packages/coding-agent/src/core/agent-session.ts · L313    313  export class AgentSession {\n 314  \treadonly agent: Agent;\n 315  \treadonly sessionManager: SessionManager;\n 316  \treadonly settingsManager: SettingsManager;\n 317  \n 318  \tprivate _scopedModels: Array<{ model: Model<any>; thinkingLevel?: ThinkingLevel }>;\n 319  \n 320  \t// Event subscription state\n 321  \tprivate _unsubscribeAgent?: () => void;\n 322  \tprivate _eventListeners: AgentSessionEventListener[] = [];\n 323  \tprivate _isAgentRunActive = false;\n 324  \tprivate _idleWaitPromise: Promise<void> | undefined;\n 325  \tprivate _resolveIdleWait: (() => void) | undefined;\n 326  \n 327  \t/** Tracks pending steering messages for UI display. Removed when delivered. */\n 328  \tprivate _steeringMessages: string[] = [];\n 329  \t/** Tracks pending follow-up messages for UI display. Removed when delivered. */\n 330  \tprivate _followUpMessages: string[] = [];   查看本地源码快照与行号  "}, {"title": "先认识 Pi 的核心对象 / 持久会话不只保存消息，还保存配置变化、摘要和扩展数据。", "url": "foundations.html#evidence", "text": "  持久会话不只保存消息，还保存配置变化、摘要和扩展数据。  packages/coding-agent/src/core/session-manager.ts · L153    153  export type SessionEntry =\n 154  \t| SessionMessageEntry\n 155  \t| ThinkingLevelChangeEntry\n 156  \t| ModelChangeEntry\n 157  \t| CompactionEntry\n 158  \t| BranchSummaryEntry\n 159  \t| CustomEntry\n 160  \t| CustomMessageEntry\n 161  \t| LabelEntry\n 162  \t| SessionInfoEntry;\n 163  \n 164  /** Raw file entry (includes header) */\n 165  export type FileEntry = SessionHeader | SessionEntry;   查看本地源码快照与行号  "}, {"title": "先认识 Pi 的核心对象 / 低层 turn 是一条 assistant 响应及其工具结果。", "url": "foundations.html#evidence", "text": "  低层 turn 是一条 assistant 响应及其工具结果。  packages/agent/src/types.ts · L452    452  \t// Turn lifecycle - a turn is one assistant response + any tool calls/results\n 453  \t| { type: \"turn_start\" }\n 454  \t| { type: \"turn_end\"; message: AgentMessage; toolResults: ToolResultMessage[] }\n 455  \t// Message lifecycle - emitted for system, user, assistant, and toolResult messages   查看本地源码快照与行号  "}, {"title": "先认识 Pi 的核心对象 / AgentTool 是能力定义，execute 才产生真实副作用。", "url": "foundations.html#evidence", "text": "  AgentTool 是能力定义，execute 才产生真实副作用。  packages/agent/src/types.ts · L406    406  export interface AgentTool<TParameters extends TSchema = TSchema, TDetails = any> extends Tool<TParameters> {\n 407  \t/** Human-readable label for UI display. */\n 408  \tlabel: string;\n 409  \t/**\n 410  \t * Optional compatibility shim for raw tool-call arguments before schema validation.\n 411  \t * Must return an object that matches `TParameters`.\n 412  \t */\n 413  \tprepareArguments?: (args: unknown) => Static<TParameters>;\n 414  \t/** Execute the tool call. Throw on failure instead of encoding errors in `content`. */\n 415  \texecute: (\n 416  \t\ttoolCallId: string,\n 417  \t\tparams: Static<TParameters>,\n 418  \t\tsignal?: AbortSignal,\n 419  \t\tonUpdate?: AgentToolUpdateCallback<TDetails>,\n 420  \t) => Promise<AgentToolResult<TDetails>>;\n 421  \t/** Recovery policy for an effect whose durable intent exists but whose outcome is unknown. */\n 422  \treplay?: \"never\" | \"safe\";\n 423  \t/**\n 424  \t * Per-tool execution mode override.\n 425  \t * - \"sequential\": this tool must execute one at a time with other tool calls.\n 426  \t * - \"parallel\": this tool can execute concurrently with other tool calls.\n 427  \t *\n 428  \t * If omitted, the default execution mode applies.\n 429  \t */\n 430  \texecutionMode?: ToolExecutionMode;\n 431  }   查看本地源码快照与行号  "}, {"title": "先认识 Pi 的核心对象 / prompt 启动 run，同一 Agent 不允许并发运行。", "url": "foundations.html#evidence", "text": "  prompt 启动 run，同一 Agent 不允许并发运行。  packages/agent/src/agent.ts · L363    363  \t/** Start a new prompt from text, a single message, or a batch of messages. */\n 364  \tasync prompt(message: AgentMessage | AgentMessage[]): Promise<void>;\n 365  \tasync prompt(input: string, images?: ImageContent[]): Promise<void>;\n 366  \tasync prompt(input: string | AgentMessage | AgentMessage[], images?: ImageContent[]): Promise<void> {\n 367  \t\tif (this.activeRun) {\n 368  \t\t\tthrow new Error(\n 369  \t\t\t\t\"Agent is already processing a prompt. Use steer() or followUp() to queue messages, or wait for completion.\",\n 370  \t\t\t);\n 371  \t\t}\n 372  \t\tconst messages = this.normalizePromptInput(input, images);\n 373  \t\tawait this.runPromptMessages(messages);\n 374  \t}\n 375  \n 376  \t/** Continue from the current transcript. The last message must be a user or tool-result message. */\n 377  \tasync continue(): Promise<void> {\n 378  \t\tif (this.activeRun) {\n 379  \t\t\tthrow new Error(\"Agent is already processing. Wait for completion before continuing.\");\n 380  \t\t}\n 381  \n 382  \t\tconst lastMessage = this._state.messages[this._state.messages.length - 1];   查看本地源码快照与行号  "}, {"title": "02 沿着职责，而非目录理解架构 / Pi CLI 的 TypeScript 主函数在哪里", "url": "architecture.html#cli-main", "text": " 仓库没有一个统管所有包的  main() ，因为它是 monorepo。若问题指用户执行的  pi  命令，npm 包的  bin  字段把  pi  指向构建产物  dist/bundle/cli.js ；对应的 TypeScript 源入口是  packages/coding-agent/src/cli.ts 。   #!/usr/bin/env node\nimport { setupCli } from \"./cli/setup.ts\";\nimport { main } from \"./main.ts\";\n\nsetupCli();\nmain(process.argv.slice(2));    cli.ts  是很薄的进程入口：先设置 CLI 进程环境，再把去掉  node  和脚本路径后的参数交给  main() 。真正的 CLI 主函数位于  packages/coding-agent/src/main.ts ：   export async function main(args: string[], options?: MainOptions)   它依次处理认证与包管理命令、解析参数、决定 interactive / print / RPC 模式、选择或创建 SessionManager、装配 AgentSessionRuntime，最后调用  InteractiveMode.run() 、 runPrintMode()  或  runRpcMode() 。因此读 CLI 启动流程应从  cli.ts → main.ts  开始，再沿所选模式向下追踪。   不要和 npm 的  main  字段混淆：  packages/coding-agent/package.json  中的  main: ./dist/index.js  是别人  import  这个 SDK 时加载的库入口； bin.pi  才是执行  pi  命令的入口。SDK 用户直接调用  createAgentSession() ，不会经过 CLI 的  main() 。 "}, {"title": "02 沿着职责，而非目录理解架构 / 从 SDK 的装配点出发", "url": "architecture.html#entry", "text": "  createAgentSession()  是理解当前编程助手的好入口。它确定工作目录，准备模型运行时、设置、资源加载器和会话管理器，然后构造 Agent，最后构造 AgentSession。它是一个装配点：把各个部件接在一起，而不是独自实现推理、文件工具或终端绘制。  具体例子：恢复旧会话时，SDK 先从 SessionManager 建立已有上下文，再尝试恢复模型与 thinking level；模型不可用时形成回退说明。这个需求属于“用户如何继续工作”，因此放在产品装配层，而不塞进每次工具执行的低层循环。  图中箭头表示主要职责协作，不是完整 import 图。AgentSession 包装 Agent，Agent 调用循环，循环通过注入的 streamFn 接入模型。UI 消费事件；不能把 UI 画成模型生成之后必经的业务处理层。 "}, {"title": "02 沿着职责，而非目录理解架构 / 这几行代码已经在使用 Pi 吗", "url": "architecture.html#sdk-usage", "text": "  是。 这段代码把 Pi 当作库嵌入当前 Node.js 进程，创建一个没有 CLI 界面的 coding-agent 会话，并用  session.prompt()  启动一次完整 run。它使用的是与 Pi CLI 相同的核心装配与执行能力，但没有终端界面、交互命令和会话选择器。     代码  实际作用       ModelRuntime.create()   加载模型目录、 ~/.pi/agent/models.json  与认证存储；后续仍需存在可用模型及其凭据。     SessionManager.inMemory()   创建只在进程内存在的会话记录；退出后不能恢复，也不会写 session JSONL。     createAgentSession(...)   装配资源加载器、设置、模型、默认工具、Agent 与 AgentSession。未指定  cwd  时使用  process.cwd() 。     session.prompt(...)   把文本变成 user message，运行一个可能包含多个 turn 和工具调用的 run，并等待它真正结算。      prompt()  的返回类型是  Promise<void> 。这里的  await  只表示“等本次 run 完成”，不会把最终文本作为字符串返回。要实时显示答案，应在调用前用  session.subscribe()  监听  message_update ；只关心完成结果时，可以在  await  后查看  session.messages 。SDK 文档的 Quick Start 因此比题目中的片段多了一段事件订阅。   const stop = session.subscribe((event) => {\n  if (event.type === \"message_update\" &&\n      event.assistantMessageEvent.type === \"text_delta\") {\n    process.stdout.write(event.assistantMessageEvent.delta);\n  }\n});\n\nawait session.prompt(\"What files are in the current directory?\");\nstop();\nsession.dispose();   若没有显式设置  tools ，当前默认内置工具是  read 、 bash 、 edit 、 write 。所以问题里的 prompt 不只是“问模型”：模型可以提出工具调用，由本地进程以当前用户权限执行，再把 toolResult 送回下一 turn。只做只读集成时，应显式选择  tools: [\"read\", \"grep\", \"find\", \"ls\"] 。   它创建了什么？ 创建的是一个  AgentSession  对象，其中持有一个  Agent  和一个内存型  SessionManager 。这不是启动一个后台 Pi CLI 进程，也不是每次  prompt()  都新建 AgentSession；多次调用  prompt()  会在同一会话中继续已有消息。 "}, {"title": "02 沿着职责，而非目录理解架构 / 三个核心边界", "url": "architecture.html#boundaries", "text": "    模块  概念别名  拥有的责任  不应从它期待什么      pi-ai  Model Runtime / Model API  模型描述、统一消息、provider 适配、响应事件  替用户决定项目任务或管理 CLI 交互    pi-agent-core  Agent Core / Agent Runtime  基础 Agent、工具循环、队列；也导出更高阶 Harness/Session 能力  所有导出都只是一个小循环    pi-coding-agent  Application / Product Shell  SDK 装配、会话策略、具体工具、扩展和运行模式  天然获得进程隔离或文件权限沙箱    pi-tui  Presentation  终端组件与显示能力  决定模型是否继续调用工具     注意 pi-agent-core 已经不只包含 agent-loop.ts。先读低层内核，是学习顺序，不是对包规模的描述。后面的进阶章节才引入 Harness 与存储契约。 "}, {"title": "02 沿着职责，而非目录理解架构 / packages 下每个目录负责什么", "url": "architecture.html#package-map", "text": " 这些目录是可独立构建和发布的 npm workspace 包，不是一次请求依次经过的流水线。下面按当前公开职责和学习优先级定位：     目录  职责  第一遍优先级       coding-agent   完整 Pi coding agent 产品：CLI/SDK 入口、AgentSession、JSONL 会话、内置文件与 shell 工具、扩展、技能、压缩，以及 interactive/print/JSON/RPC 模式。   主入口      agent   通用执行内核：Agent 状态、模型—工具循环、事件、steering/follow-up；当前还包含更通用的 Harness、Session 与存储契约。   核心      ai   统一 LLM API：Model、消息与流事件类型、provider 适配、认证解析、模型目录、用量与费用。它负责“怎样调用模型”，不负责完成 coding task。   核心      tui   通用终端 UI 库：输入编辑器、Markdown、列表、布局、键盘与鼠标、差量渲染和终端图片。它显示和收集输入，不决定 Agent 循环。  读界面时再看     session-backends   pi-agent-core 新 Session 接口的可选存储实现；当前子包  sqlite-node  用 Node 的 SQLite 保存 Session。它不是 coding-agent 现有 JSONL SessionManager 的同义目录。  进阶存储     chord   独立的应用组合运行时：用 plugin/facet、类型化 service、复制状态和远程服务边界组合多个运行环境。它可服务 Pi，也可用于无关应用。  进阶架构     protocol   实验性远程协议：定义 CBOR 帧、请求关联、取消、订阅和 server/session/attachment 路由信封，不解释 Agent 业务数据。  可先忽略     client   实验性远程客户端：在任意有序字节传输之上连接 server、发送请求、订阅服务状态并管理 attachment 路由。  可先忽略     server   实验性本地服务端：把远程连接路由到 Session 和 Chord service，管理多展示端 attachment；不是模型 provider 服务。  可先忽略     durable   面向持久 conversation/task/document 的 Pico 方向。当前公开 API 主要是持久记录契约和脱离 Agent 的内存存储，设计文档多于完整运行时实现。  了解问题即可     telemetry   与厂商无关的观测契约：span、attribute、event、schema 和内存参考实现；不自带 OpenTelemetry/Sentry exporter，也不控制业务流程。  工程化阶段     evals   Pi coding agent 的行为评估工程：组织测试案例、容器隔离、重复运行和结果比较，用于衡量行为或文档带来的效果。  评估阶段      第一次源码阅读顺序：  coding-agent/src/cli.ts → main.ts → core/agent-session.ts → agent/src/agent.ts → agent-loop.ts → ai  中实际使用的 provider。若目标是自己实现第一个 harness，到这里已经足够；先不要从 Chord、远程协议或 Durable 开始。  设计判断：拆包的主要价值是让模型适配、执行循环、产品策略和 UI 可以独立变化及复用。代价是概念会分散到多个 package，仓库结构也混合了当前主路径与演进中的新能力，因此必须从入口追踪实际调用，不能只按目录名猜架构。 "}, {"title": "02 沿着职责，而非目录理解架构 / 最小复刻时只看 agent 和 ai 吗", "url": "architecture.html#minimal-scope", "text": "  若目标是最小 Agent 执行内核，基本可以。  ai  提供模型、消息、工具调用和响应流的协议； agent  在其上实现“请求模型 → 执行工具 → 追加 toolResult → 再请求模型”的循环、运行状态和事件。  但这两个包不会自动变成一个可操作的 coding agent。你仍需写一个很薄的宿主层：选择模型和凭据，定义 system prompt，注册至少一个工具，接收用户输入，订阅输出，并在结束时释放资源。这个宿主层在职责上相当于  coding-agent  的极小子集，不需要复制 AgentSession 的全部压缩、扩展、恢复和 UI 能力。     第一遍读取  理解目标       agent/src/types.ts   AgentMessage、AgentTool、事件以及 run/turn 数据契约     agent/src/agent.ts   Agent 如何持有状态、启动 run、排队输入和取消     agent/src/agent-loop.ts   模型与工具循环如何推进和停止     agent/src/stream-fn.ts   执行内核如何注入一次模型调用     ai/src/types.ts   Model、Context、toolCall/toolResult 和流事件     ai  中一个 provider/API  统一协议怎样落到一家真实模型服务     现在可以忽略  agent/src/harness/  下更高阶的 Session、持久化与资源抽象，也可以忽略 coding-agent 的 JSONL、压缩、技能、扩展、TUI、Chord 和远程协议。等最小非持久循环成立后，再按真实需求加入取消、会话保存和上下文预算。   “只关心”指学习范围，不一定指 npm 依赖图。 当前  pi-agent-core  包本身声明了 Chord 和 telemetry 等依赖；直接安装它时包管理器会处理这些传递依赖。你不需要为了理解最小 agent loop 先阅读它们。 "}, {"title": "02 沿着职责，而非目录理解架构 / Agent Core、Runtime 与 Harness 的边界", "url": "architecture.html#naming", "text": "  agent  和  ai  是仓库中的实际目录名，但设计自己的系统时不必照抄。 agent  的公开 npm 名称已经是  pi-agent-core ；称为  Agent Core  或  Agent Runtime  能更准确表达它持有状态并推进执行。单独称为  core  又过于宽泛，离开项目上下文后看不出是哪一种核心。   Harness 通常比 Agent Core 更大。 Core/Runner 回答“下一 turn 怎样执行”；Harness 回答“模型在什么受控环境中长期工作”。后者通常进一步拥有工具与资源装配、system prompt、上下文预算、会话持久化、恢复、权限、观测和扩展机制。     层  Pi 中的近似位置  核心问题      Agent Core / Runner   agent.ts 、 agent-loop.ts   一次 run/turn 如何推进、执行工具和停止？    Agent Harness   agent/src/harness/   如何把循环、Session、资源、压缩、技能和恢复组织成通用运行环境？    Coding Harness / Product   coding-agent   如何把这些能力变成带 CLI、具体工具、设置和扩展的编码产品？     因此，如果你的模块只有模型—工具循环，叫  agent-core 、 agent-runtime  或  runner  更诚实；当它开始统一管理工具、上下文、会话和生命周期时，叫  harness  更合适。Pi 的 README 将完整 coding-agent 称为 “terminal coding harness”，而仓库也已经存在独立的  AgentHarness  接口，正好体现这一级差异。   ai  确实偏宽。它不只包含 OpenAI、Anthropic 等 provider，还拥有统一 Model、消息、上下文、流事件、认证、目录和用量，因此整个包称为  provider  会缩小其真实职责。更准确的顶层名可以是  Model Runtime 、 Model API  或  LLM ，再把具体适配实现放进  providers/ 。   agent-core/                 Agent 状态与执行循环\nharness/                    工具、上下文、Session 与运行策略\nmodel/                      统一模型契约与调用入口\nmodel/providers/openai/     具体供应商适配\napp/                        CLI 或其他产品交互   命名的判断标准不是“听起来底层”，而是包名能否说明它隔离哪种变化：Agent 循环随控制策略变化，Harness 随运行治理需求变化，Model 层随模型协议变化，provider 子层随供应商 API 变化，app 层随产品需求变化。 "}, {"title": "02 沿着职责，而非目录理解架构 / 依赖注入落实在普通函数上", "url": "architecture.html#injection", "text": " 核心循环需要“获取一次模型响应”的能力，而不是某家供应商的全局单例。 StreamFn  就是这个能力的函数契约。SDK 注入的实现调用 modelRuntime，并把超时、重试配置、请求头扩展等设置接进去。  对 Go 读者，可以类比构造函数接收一个 client 接口；对 C++ 读者，可以类比注入 callable。pi 这里不需要额外的依赖注入容器。收益是测试可提供模拟事件流，调用者可替换模型接入。代价是函数签名之外还有协议约束：返回流必须以正确的完成或错误事件结算。  当前代码同时保留  setDefaultStreamFn() ：SDK 为旧消费者安装默认实现。它说明现实工程并非总是完全显式注入。这个进程级默认值便于兼容，但会引入初始化顺序和共享状态；不能把 pi 描述成“没有任何全局状态”。 "}, {"title": "02 沿着职责，而非目录理解架构 / 机制与策略：一个有用但不绝对的分界", "url": "architecture.html#policies", "text": " 循环提供 transformContext、beforeToolCall、prepareNextTurn 等接入点；宿主决定什么时候压缩、注册哪些工具、如何响应扩展。这体现了“提供机制，允许上层配置策略”。但循环仍有默认并行执行、错误转换和退出条件，它不是完全中性的空壳。  分析推断：这一分层降低了更换模型或界面时重写控制逻辑的成本。代价是调用链跨文件，初读时不如一个大函数直观。读者应追踪“接口由谁实现、由谁调用”，而不是试图按目录顺序读完仓库。 "}, {"title": "02 沿着职责，而非目录理解架构 / 给演进中的模块一个正确位置", "url": "architecture.html#map", "text": " Chord 提供独立的服务、插件与状态复制能力；protocol、client、server 的 README 明确标注实验性；Durable 的公开入口目前导出存储契约与 MemoryStorage。这些都值得学习，但不能由包名推导出“当前 CLI 已经完全运行在这套新架构上”。  本书基础路线研究 SDK 中可见的 Agent/AgentSession 装配，进阶路线研究新模块公开契约和文档。两条路线相连，但证据等级分别标注。未来代码变动时，先重新检查入口和导出，再修改架构图。 "}, {"title": "沿着职责，而非目录理解架构 / 根配置声明 packages 下的独立 npm workspaces。", "url": "architecture.html#evidence", "text": "  根配置声明 packages 下的独立 npm workspaces。  package.json · L5      5  \t\"workspaces\": [\n   6  \t\t\"packages/*\",\n   7  \t\t\"packages/session-backends/*\",\n   8  \t\t\"packages/coding-agent/examples/extensions/with-deps\",\n   9  \t\t\"packages/coding-agent/examples/extensions/custom-provider-anthropic\",\n  10  \t\t\"packages/coding-agent/examples/extensions/custom-provider-gitlab-duo\",\n  11  \t\t\"packages/coding-agent/examples/extensions/sandbox\",\n  12  \t\t\"packages/coding-agent/examples/extensions/gondolin\"\n  13  \t],\n  14  \t\"scripts\": {\n  15  \t\t\"clean\": \"npm run clean --workspaces\",\n  16  \t\t\"build\": \"cd packages/chord && npm run build && cd ../tui && npm run build && cd ../telemetry && npm run build && cd ../ai && npm run build && cd ../durable && npm run build && cd ../agent && npm run build && cd ../session-backends/sqlite-node && npm run build && cd ../../protocol && npm run build && cd ../client && npm run build && cd ../server && npm run build && cd ../coding-agent && npm run build\",\n  17  \t\t\"build:offline\": \"cd packages/chord && npm run build && cd ../tui && npm run build && cd ../telemetry && npm run build && cd ../ai && npm run build:offline && cd ../durable && npm run build && cd ../agent && npm run build && cd ../session-backends/sqlite-node && npm run build && cd ../../protocol && npm run build && cd ../client && npm run build && cd ../server && npm run build && cd ../coding-agent && npm run build\",\n  18  \t\t\"build:native:darwin\": \"npm --prefix packages/tui run build:native:darwin\",\n  19  \t\t\"build:native:linux\": \"npm --prefix packages/tui run build:native:linux\",\n  20  \t\t\"build:native:win32\": \"npm --prefix packages/tui run build:native:win32\",   查看本地源码快照与行号  "}, {"title": "沿着职责，而非目录理解架构 / agent 包公开定位是有状态执行、工具与事件流。", "url": "architecture.html#evidence", "text": "  agent 包公开定位是有状态执行、工具与事件流。  packages/agent/README.md · L3      3  Stateful agent with tool execution and event streaming. Built on `@earendil-works/pi-ai`.\n   4  \n   5  ## Installation\n   6  \n   7  ```bash\n   8  npm install @earendil-works/pi-agent-core\n   9  ```\n  10  \n  11  ### SQLite session backends\n  12  \n  13  The SQLite session backend and the `node:sqlite` adapter live in a separate package, `@earendil-works/pi-session-backend-sqlite-node`, so the core package does not pull in runtime builtins or native SQLite dependencies by default. The backend accepts a runtime-specific SQLite factory, allowing other session backends to ship as their own packages in the future.\n  14     查看本地源码快照与行号  "}, {"title": "沿着职责，而非目录理解架构 / ai 包公开定位是统一模型 API 与 provider 能力。", "url": "architecture.html#evidence", "text": "  ai 包公开定位是统一模型 API 与 provider 能力。  packages/ai/README.md · L3      3  Unified LLM API with provider collections, automatic auth resolution, token and cost tracking, and simple context persistence and hand-off to other models mid-session.\n   4  \n   5  **Note**: This library only includes models that support tool calling (function calling), as this is essential for agentic workflows.\n   6  \n   7  ## Table of Contents\n   8  \n   9  - [Supported Providers](#supported-providers)\n  10  - [Installation](#installation)\n  11  - [Quick Start](#quick-start)\n  12  - [Providers and Models](#providers-and-models)   查看本地源码快照与行号  "}, {"title": "沿着职责，而非目录理解架构 / pi 命令的 TypeScript 进程入口把参数交给 main。", "url": "architecture.html#evidence", "text": "  pi 命令的 TypeScript 进程入口把参数交给 main。  packages/coding-agent/src/cli.ts · L6      6  main(process.argv.slice(2));   查看本地源码快照与行号  "}, {"title": "沿着职责，而非目录理解架构 / CLI 主函数负责启动准备、运行时装配和模式分派。", "url": "architecture.html#evidence", "text": "  CLI 主函数负责启动准备、运行时装配和模式分派。  packages/coding-agent/src/main.ts · L561    561  export async function main(args: string[], options?: MainOptions) {\n 562  \tresetTimings();\n 563  \tconst extensionFactories = [...builtInExtensions, ...(options?.extensionFactories ?? [])];\n 564  \tconst offlineMode = args.includes(\"--offline\") || isTruthyEnvFlag(process.env.PI_OFFLINE);\n 565  \tif (offlineMode) {\n 566  \t\tprocess.env.PI_OFFLINE = \"1\";\n 567  \t\tprocess.env.PI_SKIP_VERSION_CHECK = \"1\";\n 568  \t}\n 569  \n 570  \tif (await runAuthCommand(args)) {\n 571  \t\treturn;\n 572  \t}\n 573  \n 574  \tif (process.platform === \"win32\") {\n 575  \t\tcleanupWindowsSelfUpdateQuarantine(getPackageDir());\n 576  \t}\n 577  \tcleanupManagedInstall();\n 578     查看本地源码快照与行号  "}, {"title": "沿着职责，而非目录理解架构 / SDK 是装配点，准备模型、设置与会话资源。", "url": "architecture.html#evidence", "text": "  SDK 是装配点，准备模型、设置与会话资源。  packages/coding-agent/src/core/sdk.ts · L173    173  export async function createAgentSession(options: CreateAgentSessionOptions = {}): Promise<CreateAgentSessionResult> {\n 174  \tconst cwd = resolvePath(options.cwd ?? options.sessionManager?.getCwd() ?? process.cwd());\n 175  \tconst agentDir = options.agentDir ? resolvePath(options.agentDir) : getDefaultAgentDir();\n 176  \tlet resourceLoader = options.resourceLoader;\n 177  \n 178  \tconst authPath = options.agentDir ? join(agentDir, \"auth.json\") : undefined;\n 179  \tconst modelsPath = options.agentDir ? join(agentDir, \"models.json\") : undefined;\n 180  \tconst modelRuntime = options.modelRuntime ?? (await ModelRuntime.create({ authPath, modelsPath }));\n 181  \n 182  \tconst settingsManager = options.settingsManager ?? SettingsManager.create(cwd, agentDir);\n 183  \tconst sessionManager = options.sessionManager ?? SessionManager.create(cwd, getDefaultSessionDir(cwd, agentDir));\n 184  \n 185  \tif (!resourceLoader) {\n 186  \t\tresourceLoader = new DefaultResourceLoader({ cwd, agentDir, settingsManager });\n 187  \t\tawait resourceLoader.reload();\n 188  \t\ttime(\"resourceLoader.reload\");\n 189  \t}\n 190  \n 191  \t// Check if session has existing data to restore\n 192  \tconst existingSession = sessionManager.buildSessionContext();   查看本地源码快照与行号  "}, {"title": "沿着职责，而非目录理解架构 / prompt 校验模型与凭据，构造 user message，并等待完整 Agent run。", "url": "architecture.html#evidence", "text": "  prompt 校验模型与凭据，构造 user message，并等待完整 Agent run。  packages/coding-agent/src/core/agent-session.ts · L1250   1250  \tasync prompt(text: string, options?: PromptOptions): Promise<void> {\n1251  \t\tconst expandPromptTemplates = options?.expandPromptTemplates ?? true;\n1252  \t\tconst preflightResult = options?.preflightResult;\n1253  \t\tlet messages: AgentMessage[] | undefined;\n1254  \n1255  \t\ttry {\n1256  \t\t\t// Handle extension commands first (execute immediately, even during streaming)\n1257  \t\t\t// Extension commands manage their own LLM interaction via pi.sendMessage()\n1258  \t\t\tif (expandPromptTemplates && text.startsWith(\"/\")) {\n1259  \t\t\t\tconst handled = await this._tryExecuteExtensionCommand(text);\n1260  \t\t\t\tif (handled) {\n1261  \t\t\t\t\t// Extension command executed, no prompt to send\n1262  \t\t\t\t\tpreflightResult?.(true);\n1263  \t\t\t\t\treturn;\n1264  \t\t\t\t}\n1265  \t\t\t}\n1266  \n1267  \t\t\tif (this._compactionAbortController !== undefined) {\n1268  \t\t\t\tthrow new Error(\n1269  \t\t\t\t\t\"Cannot submit a prompt while compaction is in progress. Wait for compaction to finish and retry.\",\n1270  \t\t\t\t);\n1271  \t\t\t}   查看本地源码快照与行号  "}, {"title": "沿着职责，而非目录理解架构 / 未指定 tools 时启用 read、bash、edit、write。", "url": "architecture.html#evidence", "text": "  未指定 tools 时启用 read、bash、edit、write。  packages/coding-agent/src/core/sdk.ts · L256    256  \tconst defaultActiveToolNames: ToolName[] = [\"read\", \"bash\", \"edit\", \"write\"];\n 257  \tconst configuredDefaultToolNames = settingsManager.getDefaultTools();\n 258  \tconst allowedToolNames = options.tools ?? (options.noTools === \"all\" ? [] : undefined);\n 259  \tconst excludedToolNames = options.excludeTools;\n 260  \tconst excludedToolNameSet = excludedToolNames ? new Set(excludedToolNames) : undefined;\n 261  \tconst initialActiveToolNames = (\n 262  \t\toptions.tools ?? (options.noTools ? [] : (configuredDefaultToolNames ?? defaultActiveToolNames))\n 263  \t).filter((name) => !excludedToolNameSet?.has(name));\n 264  \n 265  \tlet agent: Agent;\n 266  \n 267  \t// Create convertToLlm wrapper that filters images if blockImages is enabled (defense-in-depth)   查看本地源码快照与行号  "}, {"title": "沿着职责，而非目录理解架构 / 产品层向 Agent 注入模型调用实现。", "url": "architecture.html#evidence", "text": "  产品层向 Agent 注入模型调用实现。  packages/coding-agent/src/core/sdk.ts · L306    306  \tagent = new Agent({\n 307  \t\tinitialState: {\n 308  \t\t\tsystemPrompt: \"\",\n 309  \t\t\tmodel,\n 310  \t\t\tthinkingLevel,\n 311  \t\t\ttools: [],\n 312  \t\t},\n 313  \t\tconvertToLlm: convertToLlmWithBlockImages,\n 314  \t\tstreamFn: async (model, context, options) => {\n 315  \t\t\tconst providerRetrySettings = settingsManager.getProviderRetrySettings();\n 316  \t\t\tconst httpIdleTimeoutMs = settingsManager.getHttpIdleTimeoutMs();\n 317  \t\t\t// SDKs treat timeout=0 as 0ms (immediate timeout), not \"no timeout\".\n 318  \t\t\t// Use max int32 to effectively disable the timeout.\n 319  \t\t\tconst effectiveTimeoutMs = httpIdleTimeoutMs === 0 ? 2147483647 : httpIdleTimeoutMs;\n 320  \t\t\tconst timeoutMs = options?.timeoutMs ?? providerRetrySettings.timeoutMs ?? effectiveTimeoutMs;\n 321  \t\t\tconst websocketConnectTimeoutMs =\n 322  \t\t\t\toptions?.websocketConnectTimeoutMs ?? settingsManager.getWebSocketConnectTimeoutMs();\n 323  \t\t\tconst headerRunner = extensionRunnerRef.current;   查看本地源码快照与行号  "}, {"title": "沿着职责，而非目录理解架构 / 默认函数是当前仍存在的共享状态。", "url": "architecture.html#evidence", "text": "  默认函数是当前仍存在的共享状态。  packages/agent/src/stream-fn.ts · L3      3  let defaultStreamFn: StreamFn | undefined;\n   4  \n   5  /**\n   6   * Configure the fallback used by Agent and low-level loops when callers omit streamFn.\n   7   *\n   8   * Hosts that provide a default model runtime can install its stream function here\n   9   * without making pi-agent-core depend on a provider catalog or compatibility layer.\n  10   */\n  11  export function setDefaultStreamFn(streamFn: StreamFn | undefined): void {\n  12  \tdefaultStreamFn = streamFn;\n  13  }\n  14  \n  15  export function getDefaultStreamFn(): StreamFn {\n  16  \tif (!defaultStreamFn) {\n  17  \t\tthrow new Error(\"No default stream function configured. Pass streamFn explicitly or call setDefaultStreamFn().\");\n  18  \t}\n  19  \treturn defaultStreamFn;\n  20  }   查看本地源码快照与行号  "}, {"title": "沿着职责，而非目录理解架构 / Durable 的实际公开导出，不能用设计文档代替。", "url": "architecture.html#evidence", "text": "  Durable 的实际公开导出，不能用设计文档代替。  packages/durable/src/index.ts · L1      1  export { MemoryStorage } from \"./memory-storage.ts\";\n   2  export type {\n   3  \tContextEdit,\n   4  \tConversationRecord,\n   5  \tCursor,\n   6  \tDocumentCreate,\n   7  \tDocumentRecord,\n   8  \tEntryDraft,\n   9  \tEntryQuery,\n  10  \tEntryRecord,\n  11  \tId,\n  12  \tInput,\n  13  \tPage,\n  14  \tSeq,\n  15  \tStorage,\n  16  \tStorageWrite,\n  17  \tStoredError,\n  18  \tTaskOutcome,\n  19  \tTaskQuery,\n  20  \tTaskRecord,\n  21  \tTaskState,\n  22  } from \"./types.ts\";\n  23  export { ROOT_CONVERSATION_ID } from \"./types.ts\";   查看本地源码快照与行号  "}, {"title": "03 一次请求，究竟经过了什么 / 输入：先成为有角色的消息", "url": "journey.html#input", "text": " Agent.prompt 接收字符串或消息。字符串会被包装为 user 消息；Agent 为本次运行建立取消控制器，并拒绝同一实例同时启动第二个 prompt。正在运行时的新输入应走 steer 或 followUp 队列。  runAgentLoop 把新消息接到已有上下文后面，发送 agent_start、turn_start 和相应消息事件。开始请求前还会对比当前可执行工具与历史中的工具声明，把变更写成 system 消息。这解决了“模型以为工具存在，但宿主已经换掉工具集合”的不一致。 "}, {"title": "03 一次请求，究竟经过了什么 / 请求边界：先变换，再转换", "url": "journey.html#boundary", "text": " streamAssistantResponse 先执行  transformContext ，仍在 AgentMessage 世界里操作；随后执行  convertToLlm ，转换为模型支持的 Message；最后 normalizeContext 形成标准请求上下文，再调用 streamFn。  为什么分两步？应用可能有 bashExecution、压缩摘要、通知等消息。调整上下文时需要理解应用语义，而 API 适配时只应面对约定好的模型消息。若把两件事合成一个到处特殊判断的序列化函数，UI 特有数据容易泄漏到模型协议。  SDK 的 convertToLlm 还会根据 blockImages 设置过滤图片。这说明“本地记录了什么”和“这次允许发什么”是两个决策。模型请求边界正是统一实施这类转换的位置。 "}, {"title": "03 一次请求，究竟经过了什么 / 响应：先收齐工具调用，再执行", "url": "journey.html#response", "text": " 模型流先产生 start，随后产生 text、thinking 或 toolcall 的增量事件。循环更新当前部分消息并发出 message_update，最终使用 response.result() 的结果结算完整消息。流式工具参数尚未收齐时，不会在每个增量上直接执行工具。  示例中，最终 assistant 内容包含 read 调用。循环提取 toolCall，执行工具批次。read 返回内容后，运行时生成带 toolCallId 的 toolResult，把结果接到上下文，再进入下一轮模型请求。调用 ID 是结果与请求的关联键，不能只用工具名配对：一次响应可能两次调用同名工具。 "}, {"title": "03 一次请求，究竟经过了什么 / 结束：无工具可做，也没有待处理消息", "url": "journey.html#ending", "text": " 第二次模型响应给出解释，没有工具调用。循环还要检查 steering 与 follow-up 消息；都没有时才发送 agent_end。若 shouldStopAfterTurn 要求停止，会在当前 turn 完成后提前退出；模型 error 或 aborted 也有明确的终止路径。   agent_end 不是“所有监听器已经完成”。  Agent 类会等待事件订阅者，然后 finishRun 才清除运行状态。日志写入或会话保存若属于被等待的监听工作，也在这次运行的结算边界内。这一细节直接影响下一次 prompt 何时能安全开始。 "}, {"title": "03 一次请求，究竟经过了什么 / 为什么不让模型直接拿文件系统", "url": "journey.html#tradeoff", "text": " 分析推断：显式的工具请求与结果保留了执行边界，使宿主能验证、记录、拦截和替换操作。代价是每次工具反馈通常需要下一轮模型调用，增加延迟和上下文占用。它适合需要依据外部结果逐步决策的任务；固定业务流程不必为了“Agent”这个名字把每一步都交给模型选择。  另一个代价是上下文增长。读取大文件会把大量结果带回模型，产品层因此需要截断、分段读取和压缩等策略。基础循环负责传递结果，结果应该多大则由具体工具和应用共同承担。 "}, {"title": "一次请求，究竟经过了什么 / 文本输入变成 user 消息。", "url": "journey.html#evidence", "text": "  文本输入变成 user 消息。  packages/agent/src/agent.ts · L406    406  \tprivate normalizePromptInput(\n 407  \t\tinput: string | AgentMessage | AgentMessage[],\n 408  \t\timages?: ImageContent[],\n 409  \t): AgentMessage[] {\n 410  \t\tif (Array.isArray(input)) {\n 411  \t\t\treturn input;\n 412  \t\t}\n 413  \n 414  \t\tif (typeof input !== \"string\") {\n 415  \t\t\treturn [input];\n 416  \t\t}\n 417  \n 418  \t\tconst content: Array<TextContent | ImageContent> = [{ type: \"text\", text: input }];\n 419  \t\tif (images && images.length > 0) {\n 420  \t\t\tcontent.push(...images);\n 421  \t\t}\n 422  \t\treturn [{ role: \"user\", content, timestamp: Date.now() }];\n 423  \t}\n 424  \n 425  \tprivate async runPromptMessages(\n 426  \t\tmessages: AgentMessage[],\n 427  \t\toptions: { skipInitialSteeringPoll?: boolean } = {},\n 428  \t): Promise<void> {   查看本地源码快照与行号  "}, {"title": "一次请求，究竟经过了什么 / 上下文变换、消息转换与模型调用的顺序。", "url": "journey.html#evidence", "text": "  上下文变换、消息转换与模型调用的顺序。  packages/agent/src/agent-loop.ts · L346    346  \t// Apply context transform if configured (AgentMessage[] → AgentMessage[])\n 347  \tlet messages = context.messages;\n 348  \tif (config.transformContext) {\n 349  \t\tmessages = await config.transformContext(messages, signal);\n 350  \t}\n 351  \n 352  \t// Convert to LLM-compatible messages (AgentMessage[] → Message[])\n 353  \tconst llmMessages = await config.convertToLlm(messages);\n 354  \n 355  \tconst llmContext = normalizeContext({ messages: llmMessages });\n 356  \n 357  \t// Resolve API key (important for expiring tokens)\n 358  \tconst resolvedApiKey =\n 359  \t\t(config.getApiKey ? await config.getApiKey(config.model.provider) : undefined) || config.apiKey;\n 360  \n 361  \tconst response = await streamFunction(config.model, llmContext, {\n 362  \t\t...config,\n 363  \t\tapiKey: resolvedApiKey,\n 364  \t\tsignal,   查看本地源码快照与行号  "}, {"title": "一次请求，究竟经过了什么 / 响应完成之后执行工具，并把结果追加到上下文。", "url": "journey.html#evidence", "text": "  响应完成之后执行工具，并把结果追加到上下文。  packages/agent/src/agent-loop.ts · L228    228  \t\t\tconst toolCalls = message.content.filter((c) => c.type === \"toolCall\");\n 229  \n 230  \t\t\tconst toolResults: ToolResultMessage[] = [];\n 231  \t\t\thasMoreToolCalls = false;\n 232  \t\t\tif (toolCalls.length > 0) {\n 233  \t\t\t\t// A \"length\" stop means the output was cut off by the token limit, so\n 234  \t\t\t\t// every tool call in the message may carry truncated arguments. Fail\n 235  \t\t\t\t// them all instead of executing potentially borked calls.\n 236  \t\t\t\tconst executedToolBatch =\n 237  \t\t\t\t\tmessage.stopReason === \"length\"\n 238  \t\t\t\t\t\t? await failToolCallsFromTruncatedMessage(toolCalls, emit)\n 239  \t\t\t\t\t\t: await executeToolCalls(currentContext, message, config, signal, emit);\n 240  \t\t\t\ttoolResults.push(...executedToolBatch.messages);\n 241  \t\t\t\thasMoreToolCalls = !executedToolBatch.terminate;\n 242  \n 243  \t\t\t\tfor (const result of toolResults) {\n 244  \t\t\t\t\tcurrentContext.messages.push(result);\n 245  \t\t\t\t\tnewMessages.push(result);\n 246  \t\t\t\t}\n 247  \t\t\t}\n 248  \n 249  \t\t\tawait emit({ type: \"turn_end\", message, toolResults });\n 250  \n 251  \t\t\tlastCompletedTurn = {\n 252  \t\t\t\tmessage,\n 253  \t\t\t\ttoolResults,\n 254  \t\t\t\tcontext: currentContext,   查看本地源码快照与行号  "}, {"title": "一次请求，究竟经过了什么 / 运行结算清理在事件处理完成后发生。", "url": "journey.html#evidence", "text": "  运行结算清理在事件处理完成后发生。  packages/agent/src/agent.ts · L544    544  \tprivate finishRun(): void {\n 545  \t\tthis._state.isStreaming = false;\n 546  \t\tthis._state.streamingMessage = undefined;\n 547  \t\tthis._state.pendingToolCalls = new Set<string>();\n 548  \t\tthis.activeRun?.resolve();\n 549  \t\tthis.activeRun = undefined;\n 550  \t}\n 551  \n 552  \t/**   查看本地源码快照与行号  "}, {"title": "04 控制权必须留在运行时 / 两层循环表达两种继续", "url": "control.html#loops", "text": " 内层循环处理工具反馈与 steering；外层循环处理 Agent 原本要停下时的 follow-up。它们代表不同意图：steering 是“下一轮请改变方向”，follow-up 是“当前工作结束后再做这件事”。  例子：模型一次请求读取 A、B 两个文件。读取期间用户说“重点看 B”。steering 不会跳过这批已提出的调用，而是在当前 turn 的工具执行完成后注入。若用户发的是“解释完再写摘要”，follow-up 会等到当前循环无工具和 steering 时才进入。  两类队列可按一次一条或一次全部排空。把输入分开是额外 API 复杂度，却避免了所有消息只有一种抢占语义。不要把 steer 翻译成“立即中断当前工具”。 "}, {"title": "04 控制权必须留在运行时 / 在 Pi 界面中如何触发", "url": "control.html#operations", "text": "    操作  运行中提交时  何时进入模型上下文      普通 Enter  作为 steering 排队  当前 assistant 响应及其工具批次结束后，在下一次模型调用前    follow-up 快捷键  作为 follow-up 排队；macOS/Linux 默认 Alt+Enter，Windows 默认 Ctrl+Q，按键可配置  当前工作已经没有工具续轮和 steering、本来将要结束时     因此你的理解基本正确：它们通常都是“上一次 run 还没结束时，用户又发了一条消息”。区别不在消息内容，而在排队语义。  steering 也不是立即打断当前 turn。模型正在流式生成时，它不会终止这次请求；工具正在执行时，它也不会撤销这批工具。它会等待当前 turn 完整结算，然后作为新的 user message 进入下一 turn。真正要停止当前模型或工具，应使用 interrupt/abort。  follow-up 表达“先完成当前任务，再做这件事”。例如当前目标是修复错误，follow-up 可以是“完成后再写一段变更摘要”。在低层循环中，它会在 Agent 原本准备发出 agent_end 的位置被取出，使活动 run 继续产生后续 turn。  Follow-up 与新 run 的真正区别     边界  Follow-up  新 run      提交时机  当前 run 仍活动时预先排队  通常在上一个 run 已完全结算、Agent 空闲后提交    agent_end  循环发现 follow-up 后暂不发送 agent_end  上一个 run 已发送 agent_end，且等待型监听器已经完成    生命周期  继续使用当前 run 的取消信号与运行边界  发出新的 agent_start，建立新的 run 生命周期    产品预处理  输入处理和模板展开已在排队时完成；不会完整重走一次空闲态 prompt 的启动流程  重新执行鉴权、压缩检查、before_agent_start、工具与系统提示准备等 run 级流程      模型是否看到不同提示词？不一定。 run 边界本身不会被编码成一条消息。若两条路径最终具有相同的 system 内容、历史消息、新 user message、工具声明、模型和上下文转换结果，那么发送给 provider 的 transcript 可以等价，模型不会知道这是 follow-up 还是新 run。  输入会在这些情况下不同：新 run 的  before_agent_start  扩展增加了消息或修改系统提示；两次 run 之间切换了模型或工具；空闲态 prompt 的预检查触发压缩；等待期间积累的 bash 或自定义消息被写入上下文。follow-up 的下一 turn 也会执行 turn 级上下文刷新和阈值压缩，所以不能简单理解为“follow-up 不压缩，新 run 才压缩”。  因此 follow-up 的核心价值是调度，而不是一种特殊推理模式：用户可以在 Agent 忙碌时预先声明下一件事，运行时保证它排在当前任务之后并自动继续。若你需要先观察结果再决定下一步，应该等待并发送新 run；若下一步与结果无关，可以提前排为 follow-up。 "}, {"title": "04 控制权必须留在运行时 / 事件既用于观察，也可能形成等待边界", "url": "control.html#events", "text": " Agent 的 processEvents 先更新内部状态，再按订阅顺序 await 每个 listener。监听者看到的是已更新的状态，监听 Promise 的完成也参与当前运行的完成。这可以让某些保存工作成为继续执行前的屏障。  低层 agentLoop 返回 EventStream，它推送事件而不等待外部异步迭代消费者处理完毕。两种 API 都产生事件，但等待语义不同。类比 Go：往一个队列发送任务，不等于等待接收者处理完；显式等待回调又是另一种契约。  分析推断：可等待监听器有利于顺序一致性，但慢监听器会拖慢主流程，抛出异常还会影响结算。事件驱动不自动意味着彻底解耦，必须问清“生产者等待谁”。 "}, {"title": "04 控制权必须留在运行时 / 取消是协作协议，不是事务回滚", "url": "control.html#cancel", "text": " Agent.abort 调用 AbortController.abort。信号继续传到模型调用、工具与钩子；这些实现必须配合检查或取消 I/O。它不是操作系统强杀线程，也不会撤销已经写入的文件。  比如文件写入已经交给底层系统，用户此刻取消，写入仍可能完成。file-mutation-queue 的测试专门覆盖“取消后的写入尚未结束时，后续写入不能抢先进入”。因此取消、I/O 完成和锁释放是三个不同时间点。  Go 的 context cancellation 是更接近的类比；Rust 的 drop future 不能直接当成相同语义，因为 JS Promise 本身不会因你不再等待就自动取消底层工作。 "}, {"title": "04 控制权必须留在运行时 / 区分工具失败和模型失败", "url": "control.html#errors", "text": "    情形  低层行为  设计意义      工具不存在、参数不合法、执行抛错  生成 isError 工具结果，通常允许模型继续  把可解释的环境反馈带回决策者    模型 stopReason 为 error / aborted  结束 turn 和当前 loop  没有可靠响应时停止推进    响应达到 length 且含工具调用  整批调用记为失败，不实际执行  避免执行看似可解析、实际不完整的参数    工具批次全部 terminate  不因该批工具自动续轮；队列仍可要求继续  批次终止提示与队列策略分开     工具错误不保证模型能恢复；模型可能继续重复失败。基础循环本身没有固定的最大轮数守卫。宿主可以用停止钩子建立预算或策略，不能将没有内置限制误读为“必然收敛”。 "}, {"title": "04 控制权必须留在运行时 / 设计风格：显式顺序优于隐藏魔法", "url": "control.html#style", "text": " 分析推断：runLoop 用普通 while、条件分支与 await 表达顺序，读者能直接找到“何时注入输入”“何时退出”。与通用工作流图引擎相比，它更容易跟踪；当生命周期选项逐渐变多，嵌套分支也会增加理解成本。  值得借鉴的是明确边界与不变量，而不是照搬两层循环的语法。需要持久化恢复时，内存循环并不足够，必须额外记录已接受工作与外部效果；这正是进阶模块需要解决的问题。 "}, {"title": "控制权必须留在运行时 / AgentSession 明确记录 steering 与 follow-up 的交付时机。", "url": "control.html#evidence", "text": "  AgentSession 明确记录 steering 与 follow-up 的交付时机。  packages/coding-agent/src/core/agent-session.ts · L1498   1498  \tasync steer(text: string, images?: ImageContent[], options?: { source?: InputSource }): Promise<void> {\n1499  \t\tawait this._queueUserInput(text, images, \"steer\", options?.source ?? \"interactive\");\n1500  \t}\n1501  \n1502  \t/**\n1503  \t * Queue a follow-up message to be processed after the agent finishes.\n1504  \t * Delivered only when agent has no more tool calls or steering messages.\n1505  \t * Expands skill commands and prompt templates. Errors on extension commands.\n1506  \t * @param images Optional image attachments to include with the message\n1507  \t * @param options Input source; defaults to interactive\n1508  \t * @throws Error if text is an extension command\n1509  \t */\n1510  \tasync followUp(text: string, images?: ImageContent[], options?: { source?: InputSource }): Promise<void> {\n1511  \t\tawait this._queueUserInput(text, images, \"followUp\", options?.source ?? \"interactive\");\n1512  \t}\n1513  \n1514  \t/**\n1515  \t * Internal: Queue a steering message (already expanded, no extension command check).\n1516  \t */\n1517  \tprivate async _queueSteer(text: string, images?: ImageContent[]): Promise<void> {\n1518  \t\tthis._steeringMessages.push(text);\n1519  \t\tthis._emitQueueUpdate();\n1520  \t\tconst content: (TextContent | ImageContent)[] = [{ type: \"text\", text }];\n1521  \t\tif (images) {   查看本地源码快照与行号  "}, {"title": "控制权必须留在运行时 / 两层循环对应不同的继续条件。", "url": "control.html#evidence", "text": "  两层循环对应不同的继续条件。  packages/agent/src/agent-loop.ts · L176    176  \t// Outer loop: continues when queued follow-up messages arrive after agent would stop\n 177  \twhile (true) {\n 178  \t\tlet hasMoreToolCalls = true;\n 179  \n 180  \t\t// Inner loop: process tool calls and steering messages\n 181  \t\twhile (hasMoreToolCalls || pendingMessages.length > 0) {\n 182  \t\t\tlet preparedMessages: AgentMessage[] = [];\n 183  \t\t\tif (lastCompletedTurn) {\n 184  \t\t\t\tconst nextTurnSnapshot = await config.prepareNextTurn?.(lastCompletedTurn);\n 185  \t\t\t\tif (nextTurnSnapshot) {\n 186  \t\t\t\t\tcurrentContext = nextTurnSnapshot.context ?? currentContext;\n 187  \t\t\t\t\tpreparedMessages = nextTurnSnapshot.messages ?? [];   查看本地源码快照与行号  "}, {"title": "控制权必须留在运行时 / follow-up 只在原本会停止的位置消费。", "url": "control.html#evidence", "text": "  follow-up 只在原本会停止的位置消费。  packages/agent/src/agent-loop.ts · L266    266  \t\t// Agent would stop here. Check for follow-up messages.\n 267  \t\tconst followUpMessages = (await config.getFollowUpMessages?.()) || [];\n 268  \t\tif (followUpMessages.length > 0) {\n 269  \t\t\t// Set as pending so inner loop processes them\n 270  \t\t\tpendingMessages = followUpMessages;\n 271  \t\t\tcontinue;\n 272  \t\t}\n 273  \n 274  \t\t// No more messages, exit\n 275  \t\tbreak;\n 276  \t}\n 277  \n 278  \tawait emit({ type: \"agent_end\", messages: newMessages });   查看本地源码快照与行号  "}, {"title": "控制权必须留在运行时 / 类级订阅者按序等待。", "url": "control.html#evidence", "text": "  类级订阅者按序等待。  packages/agent/src/agent.ts · L603    603  \t\tfor (const listener of this.listeners) {\n 604  \t\t\tawait listener(event, signal);\n 605  \t\t}\n 606  \t}   查看本地源码快照与行号  "}, {"title": "控制权必须留在运行时 / 低层事件流的 push 不等待消费者。", "url": "control.html#evidence", "text": "  低层事件流的 push 不等待消费者。  packages/ai/src/utils/event-stream.ts · L43     43  \tpush(event: T): void {\n  44  \t\tif (this.done) return;\n  45  \n  46  \t\tif (this.isComplete(event)) {\n  47  \t\t\tthis.done = true;\n  48  \t\t\tthis.resolveFinalResult(this.extractResult(event));\n  49  \t\t}\n  50  \n  51  \t\t// Deliver to waiting consumer or queue it\n  52  \t\tconst waiter = this.waiting.dequeue();\n  53  \t\tif (waiter) {\n  54  \t\t\twaiter({ value: event, done: false });\n  55  \t\t} else {\n  56  \t\t\tthis.queue.enqueue(event);\n  57  \t\t}\n  58  \t}\n  59     查看本地源码快照与行号  "}, {"title": "控制权必须留在运行时 / 已有测试覆盖取消与未完成写入之间的边界；本书不声称已执行该测试。", "url": "control.html#evidence", "text": "  已有测试覆盖取消与未完成写入之间的边界；本书不声称已执行该测试。  packages/coding-agent/test/file-mutation-queue.test.ts · L176    176  \tit(\"keeps write queue locked while an aborted write is still in flight\", async () => {\n 177  \t\tconst dir = await createTempDir();\n 178  \t\tconst filePath = join(dir, \"abort-write.txt\");\n 179  \t\tconst firstWriteStarted = createDeferred();\n 180  \t\tconst finishFirstWrite = createDeferred();\n 181  \t\tconst secondWriteStarted = createDeferred();\n 182  \t\tlet firstWriteSettled = false;\n 183  \n 184  \t\tconst writeTool = createWriteTool(dir, {\n 185  \t\t\toperations: {\n 186  \t\t\t\tmkdir: async () => {},\n 187  \t\t\t\twriteFile: async (path, content) => {\n 188  \t\t\t\t\tif (content === \"first\\n\") {\n 189  \t\t\t\t\t\tfirstWriteStarted.resolve();   查看本地源码快照与行号  "}, {"title": "05 统一模型接口，但保留差异 / 先统一运行时需要的最小语言", "url": "models.html#contract", "text": " Agent 主要需要四种消息角色，以及文本、图片、thinking、toolCall 等内容块。pi-ai 用类型表达这些数据，再用 AssistantMessageEvent 表达逐步生成过程。上层不必知道供应商在线路上如何拼装每个增量。   Model  同时携带 id、provider、api、contextWindow、maxTokens 等元数据。provider 表示模型服务来源，api 表示协议实现，它们不是同一个维度。一个服务来源可能使用兼容别家的 API，所以不能只凭供应商名推断请求形状。  真实源码保留 responseId、thoughtSignature、thinkingSignature 等供应商相关字段。它们让多轮调用能够保存必要的关联或不透明数据。统一接口不等于把所有额外字段都丢掉。 "}, {"title": "05 统一模型接口，但保留差异 / 在边界规范化，减少内部组合数", "url": "models.html#normalize", "text": " 公开 Context 允许 systemPrompt、tools 这些便利字段；规范化之后的 TranscriptContext 则把规则与工具放进 system 消息。后续 system 消息能改变规则段落和工具集合，重放这些更新得到当前状态。  想象两条历史：早期允许 read，后来又加入 write。如果只有一个当前 tools 数组，单看消息很难解释当时模型为什么选择某个工具。把声明变化锚定在对话里，提高了记录的可解释性。运行时仍保留可执行工具对象，因为函数不能作为协议声明发送。  TranscriptContext 使用一个类型标记，防止普通 Context 被随意当成规范化结果传给适配器。它是编译期约束，不是加密校验，也不会自动验证任意外部 JSON。 "}, {"title": "05 统一模型接口，但保留差异 / 用能力与兼容信息承认差异", "url": "models.html#differences", "text": " 类型中有 ApiOptionsMap、按 API 选择的 compat 配置，以及 reasoning、thinkingLevelMap 等模型描述。某个模型不支持的参数，不能仅因为统一接口里存在就认为一定有效。SDK 还会按模型能力约束 thinking level。  比如有的 API 不接受对话中途的 system 消息，适配层需要将规则变化折叠到开头；另一些允许原位表达。保持内部 transcript 统一，把转换责任放在边界，比让 Agent 针对每种 API 写循环分支更容易维护。  分析推断：代价是兼容选项会增长，统一数据也可能携带少数供应商才理解的字段。这是有意识的折中：保留表达能力，而非追求形式上最小的类型。 "}, {"title": "05 统一模型接口，但保留差异 / 流是一份协议，不只是一串 token", "url": "models.html#errors", "text": " 正常流有 start、增量、done；初始化失败可以直接产生 error。最终 AssistantMessage 带 stopReason、usage 和可能的 errorMessage。上层能用同一个结算入口读取完整结果，又能按事件更新界面。  接口之间仍有重要差别：Agent 的 StreamFn 契约要求运行失败在流中表达；pi-ai 的类型注释说明直接 streamSimple 在缺少认证时可能同步抛错。宿主桥接时必须理解这个差异，不能只看函数返回类型。  事件中的 partial 是活的累积响应，并非历史快照。保存引用再等待，看到的可能已经改变。对习惯 Rust 借用规则的读者，这提醒你：TypeScript 的 readonly 或类型注释无法替代运行时所有权隔离。 "}, {"title": "05 统一模型接口，但保留差异 / 该学什么，不该神化什么", "url": "models.html#tradeoff", "text": " 值得学习的是“稳定上层契约 + 明确转换边界 + 保留必要差异”。适配层降低更换供应商的代码成本，但不会消除模型行为差异、参数语义差异或能力差异。  阅读路线是先看消息和 StreamFn，再看 SDK 注入，最后挑一个 provider 工厂。第一遍不需要读完每家 API，也不要从巨大的生成模型目录开始。 "}, {"title": "统一模型接口，但保留差异 / 规范化的模型上下文具有类型标记。", "url": "models.html#evidence", "text": "  规范化的模型上下文具有类型标记。  packages/ai/src/types.ts · L624    624  export type TranscriptContext = {\n 625  \tmessages: Message[];\n 626  \treadonly [transcriptContextBrand]: true;\n 627  };\n 628     查看本地源码快照与行号  "}, {"title": "统一模型接口，但保留差异 / 模型身份、协议和能力是独立字段。", "url": "models.html#evidence", "text": "  模型身份、协议和能力是独立字段。  packages/ai/src/types.ts · L952    952  export interface Model<TApi extends Api> {\n 953  \tid: string;\n 954  \tname: string;\n 955  \tapi: TApi;\n 956  \tprovider: ProviderId;\n 957  \tbaseUrl: string;\n 958  \treasoning: boolean;\n 959  \t/**\n 960  \t * Maps pi thinking levels to provider/model-specific values.\n 961  \t * Missing keys use provider defaults. null marks a level as unsupported.\n 962  \t */\n 963  \tthinkingLevelMap?: ThinkingLevelMap;\n 964  \tinput: (\"text\" | \"image\")[];\n 965  \tcost: ModelCost;\n 966  \tcontextWindow: number;\n 967  \tmaxTokens: number;\n 968  \t/** Default sampling parameters for this model. See {@link StreamOptions.samplingParams}; per-request keys override these. */\n 969  \tsamplingParams?: Record<string, unknown>;\n 970  \theaders?: Record<string, string>;\n 971  \t/** Compatibility overrides for OpenAI-compatible APIs. If not set, auto-detected from baseUrl. */   查看本地源码快照与行号  "}, {"title": "统一模型接口，但保留差异 / Provider 工厂组合认证、模型目录和 API 实现。", "url": "models.html#evidence", "text": "  Provider 工厂组合认证、模型目录和 API 实现。  packages/ai/src/providers/openai.ts · L6      6  export function openaiProvider(): Provider<\"openai-responses\"> {\n   7  \treturn createProvider({\n   8  \t\tid: \"openai\",\n   9  \t\tname: \"OpenAI\",\n  10  \t\tbaseUrl: \"https://api.openai.com/v1\",\n  11  \t\tauth: { apiKey: envApiKeyAuth(\"OpenAI API key\", [\"OPENAI_API_KEY\"]) },\n  12  \t\tmodels: Object.values(OPENAI_MODELS),\n  13  \t\tapi: openAIResponsesApi(),\n  14  \t});\n  15  }   查看本地源码快照与行号  "}, {"title": "统一模型接口，但保留差异 / 循环只依赖流函数契约。", "url": "models.html#evidence", "text": "  循环只依赖流函数契约。  packages/agent/src/types.ts · L33     33  export type StreamFn = (\n  34  \tmodel: Model<Api>,\n  35  \tcontext: TranscriptContext,\n  36  \toptions?: SimpleStreamOptions,\n  37  ) => AssistantMessageEventStream | Promise<AssistantMessageEventStream>;   查看本地源码快照与行号  "}, {"title": "统一模型接口，但保留差异 / partial 的可变语义在接口文档中明示。", "url": "models.html#evidence", "text": "  partial 的可变语义在接口文档中明示。  packages/ai/src/types.ts · L638    638   * `partial` is the shared live response-so-far helper, not an event-time\n 639   * snapshot. Text and thinking blocks are empty when their `*_start` event is\n 640   * emitted and grow only through their corresponding `*_delta` events until the\n 641   * authoritative `*_end`. Redacted thinking may be complete at start and emit no\n 642   * deltas. Tool-call arguments at `toolcall_start` are provider-specific;\n 643   * `toolcall_delta` carries subsequent JSON updates.\n 644   */   查看本地源码快照与行号  "}, {"title": "06 工具是能力，也是边界 / 给模型的声明，给宿主的函数", "url": "tools.html#declaration", "text": " Tool 描述 name、description、parameters；AgentTool 在此基础上增加 execute、label、执行模式等成员。模型依靠声明选择工具和形成参数，宿主靠 execute 完成真正的操作。把函数和描述放在一个工具对象中便于注册，但发送给模型时只转换出声明。  parameters 是运行时可用的 schema。TypeScript 的类型在运行时不会自动检查模型生成的 JSON，所以运行时仍要调用 validateToolArguments。类比 Go：把 JSON 解码到结构体，仍不能省略业务字段约束；“有静态类型”不是“输入已经可信”。   toolCall ID 的来源： 更准确地说，它来自 provider 返回的工具调用协议，不必假定是神经网络自己生成。Anthropic 适配器直接采用响应中的 tool-use ID；OpenAI Responses 适配器组合 call ID 与 item ID；Google 响应缺少 ID 或发生重复时，Pi 会用工具名、时间和计数器补生成。无论来源如何，后续代码只把它作为不透明关联键。 "}, {"title": "06 工具是能力，也是边界 / 执行管线的顺序为何重要", "url": "tools.html#pipeline", "text": " prepareToolCall 先找工具，按需要 prepareArguments，再校验参数，随后调用 beforeToolCall；只有允许执行的调用才进入 execute。执行完成后 afterToolCall 可覆写部分结果，最后形成工具事件和 toolResult。  顺序有明确含义：beforeToolCall 看到的是已校验参数；不存在的工具不会调用 execute；执行抛错会转为 isError 结果；被预检拦截的调用不经过正常执行后的覆写管线。afterToolCall 的 content 覆写是整个字段替换，不是自动深合并。  输出分为 content 与 details：前者用于模型可消费的内容，后者可以承载结构化展示或记录信息。不要把大量内部诊断直接塞进 content，否则它们也会成为后续推理的输入。 "}, {"title": "06 工具是能力，也是边界 / 两种顺序：完成顺序和对话顺序", "url": "tools.html#parallel", "text": " 默认并行模式先按顺序预检全部调用，再并发执行允许的调用。假设 assistant 先提出 A 再提出 B，而 B 先完成：tool_execution_end 可先报告 B，让界面及时展示进度；随后工具结果消息仍按 A、B 的源顺序发布并进入对话。  这是 Promise.all 的一个重要性质：完成时间不固定，返回结果按输入顺序组织。它不是 CPU 多线程承诺，但可重叠异步 I/O。若批次中任一工具声明 sequential，整批采用串行路径，而非只把该工具单独锁住。  文件工具还有更细的互斥机制：相同文件的修改按规范化路径排队，不同文件仍可并行。队列是进程内机制，不是跨进程锁；测试覆盖符号链接别名和取消期间的锁持有。 "}, {"title": "06 工具是能力，也是边界 / 扩展点让产品能力可组合", "url": "tools.html#extensions", "text": " SDK 接受 customTools 与 resourceLoader，并将扩展 runner 接入上下文、请求头、请求体等钩子。仓库的 custom-compaction 示例通过 session_before_compact 提供摘要结果，说明上层可以替换策略而不重写基础循环。  这种可扩展性依赖生命周期契约：什么时候加载，回调拿到什么上下文，返回结果如何被解释，失败时谁负责处理。插件数量不是设计质量的指标；扩展是否绕过重要不变量，才是需要追问的问题。  分析推断：钩子降低了定制门槛，也增加了执行顺序和故障传播的复杂度。类型能约束一部分输入输出，但不能保证第三方回调快速、可取消或没有副作用。 "}, {"title": "06 工具是能力，也是边界 / 拦截钩子不等于沙箱", "url": "tools.html#permissions", "text": " beforeToolCall 能阻止经过该管线的工具调用，却不自动限制整个进程的网络、文件和凭据访问。仓库根 README 明确说明，pi 默认以启动进程的权限运行，没有内置的通用权限限制系统。  因此，工具 schema 解决“参数形状”，钩子解决“调用策略”，隔离环境解决“实际权限”。三者不是互相替代的方案。本书不运行工具演示的真实副作用；交互区只展示教学状态转换。 "}, {"title": "工具是能力，也是边界 / 工具结果复制调用请求的 ID，运行时不按工具名猜测配对。", "url": "tools.html#evidence", "text": "  工具结果复制调用请求的 ID，运行时不按工具名猜测配对。  packages/agent/src/agent-loop.ts · L839    839  function createToolResultMessage(finalized: FinalizedToolCallOutcome): ToolResultMessage {\n 840  \treturn {\n 841  \t\trole: \"toolResult\",\n 842  \t\ttoolCallId: finalized.toolCall.id,\n 843  \t\ttoolName: finalized.toolCall.name,\n 844  \t\t// Untyped tools (JS extensions) can return results without content; normalize\n 845  \t\t// so the null never enters session history or provider payloads.\n 846  \t\tcontent: finalized.result.content ?? [],\n 847  \t\tdetails: finalized.result.details,\n 848  \t\tusage: finalized.result.usage,\n 849  \t\tisError: finalized.isError,\n 850  \t\ttimestamp: Date.now(),\n 851  \t};   查看本地源码快照与行号  "}, {"title": "工具是能力，也是边界 / Anthropic 适配器采用 provider 响应中的 tool-use ID。", "url": "tools.html#evidence", "text": "  Anthropic 适配器采用 provider 响应中的 tool-use ID。  packages/ai/src/api/anthropic-messages.ts · L663    663  \t\t\t\t\t\t\tid: event.content_block.id,\n 664  \t\t\t\t\t\t\tname: isOAuth\n 665  \t\t\t\t\t\t\t\t? fromClaudeCodeName(event.content_block.name, currentTools)\n 666  \t\t\t\t\t\t\t\t: event.content_block.name,\n 667  \t\t\t\t\t\t\targuments: (event.content_block.input as Record<string, any>) ?? {},\n 668  \t\t\t\t\t\t\tpartialJson: \"\",\n 669  \t\t\t\t\t\t\tindex: event.index,\n 670  \t\t\t\t\t\t};\n 671  \t\t\t\t\t\toutput.content.push(block);   查看本地源码快照与行号  "}, {"title": "工具是能力，也是边界 / OpenAI Responses 适配器组合调用 ID 与响应项 ID。", "url": "tools.html#evidence", "text": "  OpenAI Responses 适配器组合调用 ID 与响应项 ID。  packages/ai/src/api/openai-responses-shared.ts · L485    485  \t\tif (item.type === \"function_call\") {\n 486  \t\t\tconst block: StreamingToolCall = {\n 487  \t\t\t\ttype: \"toolCall\",\n 488  \t\t\t\tid: `${item.call_id}|${item.id}`,\n 489  \t\t\t\tname: item.name,\n 490  \t\t\t\targuments: {},\n 491  \t\t\t\t...(item.namespace !== undefined ? { namespace: item.namespace } : {}),\n 492  \t\t\t\tpartialJson: item.arguments || \"\",\n 493  \t\t\t};\n 494  \t\t\toutput.content.push(block);\n 495  \t\t\tconst slot = {\n 496  \t\t\t\ttype: \"toolCall\",\n 497  \t\t\t\tblock,\n 498  \t\t\t\tcontentIndex: output.content.length - 1,\n 499  \t\t\t} satisfies ResponsesOutputSlot;   查看本地源码快照与行号  "}, {"title": "工具是能力，也是边界 / Google 未提供 ID 或 ID 重复时，适配器补生成唯一值。", "url": "tools.html#evidence", "text": "  Google 未提供 ID 或 ID 重复时，适配器补生成唯一值。  packages/ai/src/api/google-generative-ai.ts · L194    194  \t\t\t\t\t\t\t// Generate unique ID if not provided or if it's a duplicate\n 195  \t\t\t\t\t\t\tconst providedId = part.functionCall.id;\n 196  \t\t\t\t\t\t\tconst needsNewId =\n 197  \t\t\t\t\t\t\t\t!providedId || output.content.some((b) => b.type === \"toolCall\" && b.id === providedId);\n 198  \t\t\t\t\t\t\tconst toolCallId = needsNewId\n 199  \t\t\t\t\t\t\t\t? `${part.functionCall.name}_${Date.now()}_${++toolCallCounter}`\n 200  \t\t\t\t\t\t\t\t: providedId;\n 201  \n 202  \t\t\t\t\t\t\tconst toolCall: ToolCall = {\n 203  \t\t\t\t\t\t\t\ttype: \"toolCall\",\n 204  \t\t\t\t\t\t\t\tid: toolCallId,\n 205  \t\t\t\t\t\t\t\tname: part.functionCall.name || \"\",\n 206  \t\t\t\t\t\t\t\targuments: (part.functionCall.args as Record<string, any>) ?? {},\n 207  \t\t\t\t\t\t\t\t...(part.thoughtSignature && { thoughtSignature: part.thoughtSignature }),   查看本地源码快照与行号  "}, {"title": "工具是能力，也是边界 / 校验发生在 beforeToolCall 之前。", "url": "tools.html#evidence", "text": "  校验发生在 beforeToolCall 之前。  packages/agent/src/agent-loop.ts · L680    680  \t\tconst validatedArgs = validateToolArguments(tool, preparedToolCall);\n 681  \t\tif (config.beforeToolCall) {\n 682  \t\t\tconst beforeResult = await config.beforeToolCall(\n 683  \t\t\t\t{\n 684  \t\t\t\t\tassistantMessage,\n 685  \t\t\t\t\ttoolCall,\n 686  \t\t\t\t\targs: validatedArgs,\n 687  \t\t\t\t\tcontext: currentContext,\n 688  \t\t\t\t},\n 689  \t\t\t\tsignal,\n 690  \t\t\t);\n 691  \t\t\tif (signal?.aborted) {\n 692  \t\t\t\treturn {\n 693  \t\t\t\t\tkind: \"immediate\",\n 694  \t\t\t\t\tresult: createErrorToolResult(\"Operation aborted\"),\n 695  \t\t\t\t\tisError: true,\n 696  \t\t\t\t};   查看本地源码快照与行号  "}, {"title": "工具是能力，也是边界 / 并发完成后，结果按源顺序发布。", "url": "tools.html#evidence", "text": "  并发完成后，结果按源顺序发布。  packages/agent/src/agent-loop.ts · L602    602  \tconst orderedFinalizedCalls = await Promise.all(\n 603  \t\tfinalizedCalls.map((entry) => (typeof entry === \"function\" ? entry() : Promise.resolve(entry))),\n 604  \t);\n 605  \tconst messages: ToolResultMessage[] = [];\n 606  \tfor (const finalized of orderedFinalizedCalls) {\n 607  \t\tconst toolResultMessage = createToolResultMessage(finalized);\n 608  \t\tawait emitToolResultMessage(toolResultMessage, emit);\n 609  \t\tmessages.push(toolResultMessage);\n 610  \t}\n 611  \n 612  \treturn {\n 613  \t\tmessages,   查看本地源码快照与行号  "}, {"title": "工具是能力，也是边界 / 单个串行声明使整批走串行分支。", "url": "tools.html#evidence", "text": "  单个串行声明使整批走串行分支。  packages/agent/src/agent-loop.ts · L472    472  \tconst hasSequentialToolCall = toolCalls.some(\n 473  \t\t(tc) => currentContext.tools?.find((t) => t.name === tc.name)?.executionMode === \"sequential\",\n 474  \t);\n 475  \tif (config.toolExecution === \"sequential\" || hasSequentialToolCall) {\n 476  \t\treturn executeToolCallsSequential(currentContext, assistantMessage, toolCalls, config, signal, emit);\n 477  \t}\n 478  \treturn executeToolCallsParallel(currentContext, assistantMessage, toolCalls, config, signal, emit);\n 479  }   查看本地源码快照与行号  "}, {"title": "工具是能力，也是边界 / 同文件操作有进程内队列。", "url": "tools.html#evidence", "text": "  同文件操作有进程内队列。  packages/coding-agent/src/core/tools/file-mutation-queue.ts · L32     32  export async function withFileMutationQueue<T>(filePath: string, fn: () => Promise<T>): Promise<T> {\n  33  \tconst registration = registrationQueue.then(async () => {\n  34  \t\tconst key = await getMutationQueueKey(filePath);\n  35  \t\tconst currentQueue = fileMutationQueues.get(key) ?? Promise.resolve();\n  36  \n  37  \t\tlet releaseNext!: () => void;\n  38  \t\tconst nextQueue = new Promise<void>((resolveQueue) => {\n  39  \t\t\treleaseNext = resolveQueue;\n  40  \t\t});\n  41  \t\tconst chainedQueue = currentQueue.then(() => nextQueue);\n  42  \t\tfileMutationQueues.set(key, chainedQueue);\n  43  \n  44  \t\treturn { key, currentQueue, chainedQueue, releaseNext };\n  45  \t});\n  46  \tregistrationQueue = registration.then(\n  47  \t\t() => undefined,\n  48  \t\t() => undefined,\n  49  \t);\n  50  \n  51  \tconst { key, currentQueue, chainedQueue, releaseNext } = await registration;\n  52  \tawait currentQueue;\n  53  \ttry {\n  54  \t\treturn await fn();\n  55  \t} finally {\n  56  \t\treleaseNext();\n  57  \t\tif (fileMutationQueues.get(key) === chainedQueue) {\n  58  \t\t\tfileMutationQueues.delete(key);\n  59  \t\t}\n  60  \t}\n  61  }   查看本地源码快照与行号  "}, {"title": "工具是能力，也是边界 / 权限边界是项目明示的限制。", "url": "tools.html#evidence", "text": "  权限边界是项目明示的限制。  README.md · L42     42  Pi does not include a built-in permission system for restricting filesystem, process, network, or credential access. By default, it runs with the permissions of the user and process that launched it.   查看本地源码快照与行号  "}, {"title": "07 记住历史，不等于发送全部历史 / 三个容易混淆的对象", "url": "context.html#three", "text": "    对象  回答的问题  例子      会话记录  曾经发生过什么？  消息、模型变更、压缩记录、分支摘要    模型上下文  下一次请求交给模型什么？  规则、摘要、近期消息与工具结果    运行状态  现在正在做什么？  isStreaming、部分响应、pendingToolCalls     例如 UI 正在显示的半句响应属于运行中的部分消息；一次压缩后，磁盘里仍可保留旧消息，但下一次模型请求只包含摘要和保留部分。将它们当成同一个数组，会使恢复、展示和预算管理互相牵制。 "}, {"title": "07 记住历史，不等于发送全部历史 / session 文件保存的不是 AgentSession", "url": "context.html#storage", "text": "  问题：  AgentSession  包含工具实现、事件监听器、取消控制器和正在执行的 Promise，不能可靠地直接序列化。Pi 因此不保存这个对象，而是让  SessionManager  保存足以恢复工作的记录。  默认路径是  ~/.pi/agent/sessions/--<编码后的工作目录>--/<时间>_<session-id>.jsonl 。JSONL 表示“一行一个 JSON 对象”：第一行是 header，记录版本、session ID、创建时间、cwd 和可选的父 session 文件；其余行是追加的 entry。   {\"type\":\"session\",\"version\":3,\"id\":\"...\",\"cwd\":\"/project\"}\n{\"type\":\"message\",\"id\":\"a1\",\"parentId\":null,\"message\":{\"role\":\"user\",\"content\":\"...\"}}\n{\"type\":\"message\",\"id\":\"b2\",\"parentId\":\"a1\",\"message\":{\"role\":\"assistant\",\"content\":[...]}}\n{\"type\":\"model_change\",\"id\":\"c3\",\"parentId\":\"b2\",\"provider\":\"...\",\"modelId\":\"...\"}   Pi 当前保存 message、模型与 thinking level 变化、压缩摘要、分支摘要、标签、会话名称和扩展数据。assistant 消息中还可能包含 toolCall、usage 和终止原因；toolResult 保存调用 ID、输出与错误状态。每个 entry 的  id + parentId  形成树，文件最后追加的 entry 通常是恢复时的活动 leaf。  它不保存可执行的工具函数、监听器、网络连接、AbortController 或流式中的  pending  assistant 消息。恢复过程是：读取 JSONL 并迁移旧版本 → 建立 ID 索引 → 从 leaf 沿 parentId 选出当前分支 → 应用最近的压缩记录 → 得到 messages、model 与 thinking level → 创建新的 Agent 和 AgentSession。新会话还会延迟到出现第一条完整 assistant 消息后才真正创建文件，避免只留下未完成的空会话。   与 Codex 对比（2026-09-20）： OpenAI 官方文档确认 Codex 将本地状态放在  CODEX_HOME （默认  ~/.codex ），session transcript 位于  $CODEX_HOME/sessions ，归档位于  archived_sessions ；可配置的本地历史还涉及  history.jsonl 。本机当前实现可观察到按年月日组织的每 session JSONL，按顺序记录  session_meta 、 turn_context 、 response_item 、 event_msg 、 compacted  和 token usage 等事件。后者是实现观察，不是稳定公开格式。  两者都选择追加事件而不是序列化整个运行对象，但关注点不同：Pi 的公开 session 格式用  parentId  直接表达对话分支；当前 Codex 记录更像带 ordinal 的执行轨迹，包含每 turn 的环境、权限、模型响应项和用量。实现自己的 harness 时，先保存可恢复的语义事件和外部副作用结果；不要从第一天复制某个产品的全部诊断字段。  Codex 位置依据：OpenAI 官方文档《Troubleshooting》和《Advanced Configuration》（2026-09-20 核对）。Pi 格式依据本仓库  session-manager.ts  与  docs/session-format.md 。 "}, {"title": "07 记住历史，不等于发送全部历史 / 会话树表达探索历史", "url": "context.html#tree", "text": " 项目文档：当前 coding-agent 会话以 JSONL 保存，条目通过 id 与 parentId 形成树，当前位置用 leaf 表达。JSONL 是逐行 JSON 的存储格式；树是条目之间的逻辑关系，两者并不冲突。  从一个早期问题尝试两条解决路线时，线性聊天必须复制或删除历史，树可以保留共同祖先并切换路径。模型上下文由所选路径构建，不会自动混合所有分支。离开分支时的摘要可以把有用进展带回新路径，但它不等于把两个程序状态自动合并。  这里也不是 Git 工作树：切换会话分支不意味着磁盘文件同步回到旧版本。对 C++/Rust 工程师，这是最值得防止的误解。 "}, {"title": "07 记住历史，不等于发送全部历史 / 压缩的基本步骤", "url": "context.html#compact", "text": " shouldCompact 比较 contextTokens 与 contextWindow − reserveTokens，为后续生成保留空间。prepareCompaction 找到保留边界，收集需要摘要的旧消息；compact 生成结构化摘要；产品层保存压缩条目并重建上下文。  切点不能任意落在 toolResult 前面，否则模型会收到没有对应调用的结果。源码选择用户类消息或 assistant 消息作为有效切点；若在 assistant 工具调用前切开，它后面的结果一起保留。遇到一个特别长的用户轮次，还会单独摘要被切掉的轮次前缀。  前次摘要也参与后续压缩；文件操作信息会累计。这里的目标不是无损压缩，而是保留继续任务所需的目标、约束、进展和关键决定。摘要依然由模型生成，可能遗漏重要内容。 "}, {"title": "07 记住历史，不等于发送全部历史 / 预算与失败也属于摘要设计", "url": "context.html#failure", "text": " estimateContextTokens 尽量使用最后一次有效 usage，再估算其后的消息；没有有效 usage 时估算全部消息。字符数除以四等规则是启发式，不是实际 tokenizer。特别对不同语言和多模态输入，不能把它当作精确计量保证。  摘要响应达到 length 时 getSummarizationFailure 会拒绝把不完整内容当作检查点；摘要如果试图调用工具，也会被拒绝。为什么如此保守？原始任务可以在工具错误后继续尝试，但错误摘要一旦成为后续上下文，会持续污染之后的推理。  源码将选切点、组织摘要输入、模型调用和存储职责分开。文件顶部虽称 pure functions，摘要函数实际上会调用模型；应按函数逐个判断副作用，不照单全收标题。 "}, {"title": "07 记住历史，不等于发送全部历史 / 用可恢复记录换取有限的模型视野", "url": "context.html#cost", "text": " 分析推断：保留完整会话并为模型构建投影，使人类审计与模型预算不必使用同一种表示。代价是多了一套重建规则，摘要有成本和信息损失，压缩后的模型也无法“凭空回忆”未被保留的细节。  上下文工程在这里不是神秘技巧，而是信息选择与一致性工程：哪些数据保留、何时变换、调用和结果怎样保持配对、失败后从哪里恢复。先理解这四件事，再讨论更复杂的长期记忆或检索系统。 "}, {"title": "记住历史，不等于发送全部历史 / session 文件的 header 与条目类型，证明文件不是 AgentSession 对象快照。", "url": "context.html#evidence", "text": "  session 文件的 header 与条目类型，证明文件不是 AgentSession 对象快照。  packages/coding-agent/src/core/session-manager.ts · L39     39  export interface SessionHeader {\n  40  \ttype: \"session\";\n  41  \tversion?: number; // v1 sessions don't have this\n  42  \tid: string;\n  43  \ttimestamp: string;\n  44  \tcwd: string;\n  45  \tparentSession?: string;\n  46  }\n  47  \n  48  export interface NewSessionOptions {\n  49  \tid?: string;\n  50  \tparentSession?: string;\n  51  }\n  52  \n  53  export interface SessionEntryBase {\n  54  \ttype: string;\n  55  \tid: string;\n  56  \tparentId: string | null;   查看本地源码快照与行号  "}, {"title": "记住历史，不等于发送全部历史 / SessionManager 用追加日志和 parentId 树管理持久记录。", "url": "context.html#evidence", "text": "  SessionManager 用追加日志和 parentId 树管理持久记录。  packages/coding-agent/src/core/session-manager.ts · L857    857   * Manages conversation sessions as append-only trees stored in JSONL files.\n 858   *\n 859   * Each session entry has an id and parentId forming a tree structure. The \"leaf\"\n 860   * pointer tracks the current position. Appending creates a child of the current leaf.\n 861   * Branching moves the leaf to an earlier entry, allowing new branches without\n 862   * modifying history.\n 863   *\n 864   * Use buildSessionContext() to get the resolved message list for the LLM, which\n 865   * handles compaction summaries and follows the path from root to current leaf.\n 866   */\n 867  export class SessionManager {\n 868  \tprivate sessionId: string = \"\";\n 869  \tprivate sessionFile: string | undefined;\n 870  \tprivate sessionDir: string;\n 871  \tprivate cwd: string;\n 872  \tprivate persist: boolean;   查看本地源码快照与行号  "}, {"title": "记住历史，不等于发送全部历史 / 恢复时先从记录构建上下文，再创建 Agent 与 AgentSession。", "url": "context.html#evidence", "text": "  恢复时先从记录构建上下文，再创建 Agent 与 AgentSession。  packages/coding-agent/src/core/sdk.ts · L192    192  \tconst existingSession = sessionManager.buildSessionContext();\n 193  \tconst hasExistingSession = existingSession.messages.length > 0;\n 194  \tconst hasThinkingEntry = sessionManager.getBranch().some((entry) => entry.type === \"thinking_level_change\");\n 195  \n 196  \tlet model = options.model;\n 197  \tlet modelFallbackMessage: string | undefined;\n 198  \n 199  \t// If session has data, try to restore model from it\n 200  \tif (!model && hasExistingSession && existingSession.model) {\n 201  \t\tconst restoredModel = modelRuntime.getModel(existingSession.model.provider, existingSession.model.modelId);\n 202  \t\tif (restoredModel && modelRuntime.hasConfiguredAuth(restoredModel.provider)) {\n 203  \t\t\tmodel = restoredModel;\n 204  \t\t}\n 205  \t\tif (!model) {\n 206  \t\t\tmodelFallbackMessage = `Could not restore model ${existingSession.model.provider}/${existingSession.model.modelId}`;\n 207  \t\t}\n 208  \t}\n 209  \n 210  \t// If still no model, use findInitialModel (checks settings default, then provider defaults)\n 211  \tif (!model) {   查看本地源码快照与行号  "}, {"title": "记住历史，不等于发送全部历史 / 压缩触发阈值。", "url": "context.html#evidence", "text": "  压缩触发阈值。  packages/coding-agent/src/core/compaction/compaction.ts · L250    250  export function shouldCompact(contextTokens: number, contextWindow: number, settings: CompactionSettings): boolean {\n 251  \tif (!settings.enabled) return false;\n 252  \treturn contextTokens > contextWindow - settings.reserveTokens;\n 253  }   查看本地源码快照与行号  "}, {"title": "记住历史，不等于发送全部历史 / 不能在工具结果处单独切开。", "url": "context.html#evidence", "text": "  不能在工具结果处单独切开。  packages/coding-agent/src/core/compaction/compaction.ts · L323    323  function isCutPointMessage(message: AgentMessage): boolean {\n 324  \tswitch (message.role) {\n 325  \t\tcase \"user\":\n 326  \t\tcase \"assistant\":\n 327  \t\tcase \"bashExecution\":\n 328  \t\tcase \"custom\":\n 329  \t\tcase \"branchSummary\":\n 330  \t\tcase \"compactionSummary\":\n 331  \t\t\treturn true;\n 332  \t\tcase \"toolResult\":\n 333  \t\t\treturn false;\n 334  \t}\n 335  \treturn false;\n 336  }\n 337  \n 338  function isTurnStartMessage(message: AgentMessage): boolean {\n 339  \tswitch (message.role) {   查看本地源码快照与行号  "}, {"title": "记住历史，不等于发送全部历史 / 输出达到上限时拒绝不完整摘要。", "url": "context.html#evidence", "text": "  输出达到上限时拒绝不完整摘要。  packages/coding-agent/src/core/compaction/compaction.ts · L557    557  export function getSummarizationFailure(response: AssistantMessage, label: string): string | undefined {\n 558  \tif (response.stopReason === \"error\") {\n 559  \t\treturn `${label} failed: ${response.errorMessage || \"Unknown error\"}`;\n 560  \t}\n 561  \tif (response.stopReason === \"length\") {\n 562  \t\treturn `${label} failed: generation hit the token cap and the summary is incomplete`;\n 563  \t}\n 564  \treturn undefined;\n 565  }   查看本地源码快照与行号  "}, {"title": "记住历史，不等于发送全部历史 / 应用摘要转换成模型可消费消息。", "url": "context.html#evidence", "text": "  应用摘要转换成模型可消费消息。  packages/coding-agent/src/core/messages.ts · L176    176  \t\t\t\tcase \"compactionSummary\":\n 177  \t\t\t\t\treturn {\n 178  \t\t\t\t\t\trole: \"user\",\n 179  \t\t\t\t\t\tcontent: [\n 180  \t\t\t\t\t\t\t{ type: \"text\" as const, text: COMPACTION_SUMMARY_PREFIX + m.summary + COMPACTION_SUMMARY_SUFFIX },\n 181  \t\t\t\t\t\t],\n 182  \t\t\t\t\t\ttimestamp: m.timestamp,\n 183  \t\t\t\t\t};\n 184  \t\t\t\tcase \"system\":\n 185  \t\t\t\tcase \"user\":   查看本地源码快照与行号  "}, {"title": "记住历史，不等于发送全部历史 / 当前公开文档定义路径、版本和 JSONL 条目格式。", "url": "context.html#evidence", "text": "  当前公开文档定义路径、版本和 JSONL 条目格式。  packages/coding-agent/docs/session-format.md · L3      3  Sessions are stored as JSONL (JSON Lines) files. Each line is a JSON object with a `type` field. Session entries form a tree structure via `id`/`parentId` fields, enabling in-place branching without creating new files.\n   4  \n   5  ## File Location\n   6  \n   7  ```\n   8  ~/.pi/agent/sessions/--<path>--/<timestamp>_<session-id>.jsonl\n   9  ```\n  10     查看本地源码快照与行号  "}, {"title": "08 从本地助手走向可组合系统 / 当前产品：资源跟随会话生命周期", "url": "infrastructure.html#lifecycle", "text": " AgentSessionRuntime 持有 session 及绑定工作目录的服务。切换项目时，它先取消并等待当前会话，再清理旧服务并构造新运行时；只替换 messages 会让旧工具闭包和目录设置泄漏到新项目。代价是新运行时创建失败时旧运行时已经拆除，因此调用者仍要处理非原子切换。 "}, {"title": "08 从本地助手走向可组合系统 / Chord：需要多运行环境时再读", "url": "infrastructure.html#chord", "text": " 项目文档：Chord 用 facet、类型化服务和依赖顺序组合 worker 与展示端能力，并从权威端向消费端复制状态。它解决多环境装配和观察问题，不是基础 Agent 循环的必需层，也不是允许任意多端同时写入的 CRDT。 "}, {"title": "08 从本地助手走向可组合系统 / Protocol：跨进程才需要的路由层", "url": "infrastructure.html#protocol", "text": " 实验模块文档：pi-protocol 负责请求关联、目标、取消和订阅帧；attachmentId 防止旧界面的延迟请求误投到新挂接。断连只说明本地不再等待，远端工作可能已经执行，因此默认不自动重放有副作用的请求。 "}, {"title": "08 从本地助手走向可组合系统 / Client 与 Server：远程访问 Session 的两端", "url": "infrastructure.html#client-server", "text": "  问题： 当 UI 和 Agent Harness 不在同一个进程时，UI 不能直接拿到内存中的 Session 对象，也不能把任意网络连接都当作当前会话。Pi 的实验栈把连接、路由和业务服务分开。     包  负责什么  不负责什么       pi-client   通过应用提供的有序字节传输连接服务端；校验 serverId；关联 request/response；发出取消；订阅 Chord service；保存当前 Session attachment 路由。  不执行 Agent，不保存权威 Session，不自动理解 transcript 或工具。     pi-protocol   定义 CBOR 帧、握手、请求/响应/取消/订阅信封，以及 serverId、sessionId、attachmentId 组成的目标地址。  不规定 Chord service 的业务参数，也不提供认证和自动重连策略。     pi-server   监听经过认证的字节连接；验证请求目标；管理每个展示连接的 attachment；把不透明的 service 调用转发给目标 Session endpoint；在关闭时释放句柄。  不是模型供应商服务，不自行实现 Agent 业务，也不决定 Session 如何持久化。     一个具体轨迹是：远程界面创建 Client → 与指定 serverId 握手 → 通过服务端管理 service 请求 attach 某个 sessionId → Server 创建该展示端专属的 attachmentId 并异步通知 Client → 此后的 Session 请求必须携带完整三元组 → Server 校验仍是当前挂接后，才把 Chord service 调用转给 Session worker。  为什么除了 sessionId 还需要 attachmentId？同一个 Session 可以被多个展示端观察，一个连接也可能先后切换 Session。若旧请求在网络中延迟，只检查 sessionId 可能把它误投给新的挂接；server 生成的 attachmentId 把请求限定在“这一次活跃展示关系”中。  连接断开时，Client 会在本地拒绝 pending request，并清除 live attachment，但 Server 已经接收的工作仍可能完成。因此 Client 不自动重连和重放请求；调用方需要重新连接、重新 attach，并且只重试能证明安全的操作。这是远程副作用系统的核心难点，不是普通网络错误处理细节。   当前集成状态：  coding-agent  对 client、protocol、server 的导入集中在  src/experimental/  及实验测试；普通 CLI 主流程使用的是本地 AgentSessionRuntime。这里研究的是演进中的远程架构，不是当前每次  pi  请求的默认路径。 "}, {"title": "08 从本地助手走向可组合系统 / Durable：恢复的核心是未知副作用", "url": "infrastructure.html#durable", "text": " 当前公开入口主要是 MemoryStorage、记录类型与存储契约，不能把设计文档中的完整运行时当成当前默认路径。  分析推断：如果文件已经写完而完成记录尚未落盘，恢复时既不能假定成功，也不能盲目重试。持久化 Harness 必须一起设计操作身份、幂等性和恢复策略；只有数据库并不等于 exactly-once。 "}, {"title": "08 从本地助手走向可组合系统 / 模块地图与引入成本", "url": "infrastructure.html#matrix", "text": "    模块组  解决的问题  学习重点 / 代价      coding-agent / tui  用户如何操作和观察任务  生命周期、事件展示；界面状态不等于任务状态    agent / Harness / Session  执行与会话能力  基础循环之外，还有存储、恢复与上下文边界    chord  能力组合与状态复制  依赖图、激活和释放；额外间接层    protocol / client / server  跨进程路由与连接  挂接身份、断连与结果未知；实验性接口    durable / session-backends  不同层次的记录与存储实现  核对具体接口，不假定互相替换或已统一    telemetry / evals  运行观测与效果评估  观测不能代替状态；本书仅给模块定位，不深入实现     分析推断：这些复杂度在多进程、多界面与恢复需求出现后有价值。最值得学习的并非“项目应该有这么多包”，而是每增加一层都能说清它隔离了哪种变化。 "}, {"title": "从本地助手走向可组合系统 / 切换时先等待当前运行，再释放上下文。", "url": "infrastructure.html#evidence", "text": "  切换时先等待当前运行，再释放上下文。  packages/coding-agent/src/core/agent-session-runtime.ts · L167    167  \tprivate async teardownCurrent(reason: SessionShutdownEvent[\"reason\"], targetSessionFile?: string): Promise<void> {\n 168  \t\t// Settle any active response first so the aborted turn (including tool\n 169  \t\t// results) is persisted to the outgoing session before it is replaced.\n 170  \t\tawait this.session.abort();\n 171  \t\tawait emitSessionShutdownEvent(this.session.extensionRunner, {\n 172  \t\t\ttype: \"session_shutdown\",\n 173  \t\t\treason,\n 174  \t\t\ttargetSessionFile,\n 175  \t\t});\n 176  \t\tthis.beforeSessionInvalidate?.();\n 177  \t\tthis.session.dispose();\n 178  \t}   查看本地源码快照与行号  "}, {"title": "从本地助手走向可组合系统 / 项目文档：服务声明与 facet 生命周期。", "url": "infrastructure.html#evidence", "text": "  项目文档：服务声明与 facet 生命周期。  packages/chord/README.md · L18     18  - **Plugins** are synchronous setup units that declare the services they provide\n  19    and require. After every plugin has declared its shape, a host validates the\n  20    complete dependency graph, binds services, activates providers before consumers,\n  21    and disposes resources in reverse dependency order.  These units are called\n  22    *facets*.\n  23  \n  24  - **Facets** are parts of a plugin.  Each facet is bundled up separately and runs\n  25    in the process or environment where it's supposed to run.  You can use facets\n  26    to split a plugin into separate pieces that need to be loaded into different\n  27    processes and environments (think backend, browser, TUI etc.)\n  28  \n  29  - **Services** are typed, stable tokens with either one provider (**singleton**)\n  30    or dynamic keyed instances (**keyed**).  A service can be process-local, with   查看本地源码快照与行号  "}, {"title": "从本地助手走向可组合系统 / Client 断连后不自动重放可能已有副作用的请求。", "url": "infrastructure.html#evidence", "text": "  Client 断连后不自动重放可能已有副作用的请求。  packages/client/README.md · L34     34  On disconnect or disposal, pending requests reject locally, but accepted work may still complete remotely before the attachment is released. The client clears its live attachment route. It never reconnects or replays requests automatically. After disconnection, call `reconnect()`, attach through the application's management service again, and explicitly repeat only operations known to be safe.\n  35  \n  36  The experimental local coordinator only provides a stable endpoint and relays traffic. Replaceable server processes own Session and worker lifecycle outside the public client protocol.\n  37  \n  38  Call transport handlers as follows:\n  39  \n  40  - `handlers.onData(chunk)` for inbound bytes;\n  41  - `handlers.onClose()` for an orderly terminal close;   查看本地源码快照与行号  "}, {"title": "从本地助手走向可组合系统 / Server 用展示端 attachment 隔离多端和过期路由。", "url": "infrastructure.html#evidence", "text": "  Server 用展示端 attachment 隔离多端和过期路由。  packages/server/README.md · L15     15  A Session may have multiple presentation attachments. Repeating `attach` from one connection is idempotent; every successful attachment has a server-generated `attachmentId` delivered only as routing control data. Session requests carry `{ serverId, sessionId, attachmentId }`, and the server rejects stale or mismatched routes. Losing a connection rejects its local responses but releases its attachment only after admitted service calls settle. The host decides when zero presentation demand and worker-local Harness activity permit worker retirement. Server shutdown closes every routed Session handle, releasing its worker and Session writer ownership.\n  16  \n  17  ```ts\n  18  import { randomUUID } from \"node:crypto\";\n  19  import { MemorySessionRepo, type Session } from \"@earendil-works/pi-agent-core\";\n  20  import {\n  21    type RoutedServerServiceHost,\n  22    type RoutedSessionHandle,\n  23    type ServerHost,\n  24    SessionAmbiguousError,   查看本地源码快照与行号  "}, {"title": "从本地助手走向可组合系统 / 实验协议的验证与认证边界。", "url": "infrastructure.html#evidence", "text": "  实验协议的验证与认证边界。  packages/protocol/README.md · L39     39  All envelope schemas reject unknown object properties, and codecs recursively reject non-JSON opaque payloads, including non-finite numbers, byte arrays, `undefined`, prototypes, and cycles. Envelope violations, malformed CBOR, and invalid framing throw `ProtocolValidationError`. Payload-specific adapters must perform their own semantic validation after decoding. Transports must preserve byte order. Peer authentication and authenticated service contexts are not implemented by the experimental transport.\n  40  \n  41  Default limits are 16 MiB per CBOR payload/frame, 1,000,000 array elements or map entries, and 64 nested item levels. The protocol is experimental and has no compatibility guarantees.   查看本地源码快照与行号  "}, {"title": "从本地助手走向可组合系统 / 目前实际公开的持久存储契约。", "url": "infrastructure.html#evidence", "text": "  目前实际公开的持久存储契约。  packages/durable/src/index.ts · L1      1  export { MemoryStorage } from \"./memory-storage.ts\";\n   2  export type {\n   3  \tContextEdit,\n   4  \tConversationRecord,\n   5  \tCursor,\n   6  \tDocumentCreate,\n   7  \tDocumentRecord,\n   8  \tEntryDraft,\n   9  \tEntryQuery,\n  10  \tEntryRecord,\n  11  \tId,\n  12  \tInput,\n  13  \tPage,\n  14  \tSeq,\n  15  \tStorage,\n  16  \tStorageWrite,\n  17  \tStoredError,\n  18  \tTaskOutcome,\n  19  \tTaskQuery,\n  20  \tTaskRecord,\n  21  \tTaskState,\n  22  } from \"./types.ts\";\n  23  export { ROOT_CONVERSATION_ID } from \"./types.ts\";   查看本地源码快照与行号  "}, {"title": "09 把实现读成设计判断 / 原则一：让变化在边界处收敛", "url": "principles.html#contracts", "text": " 问题：模型 API 和应用消息都在变化。例子：扩展加入一种 custom 消息，供应商又不接受中途 system 更新。方案：应用消息先由 convertToLlm 转换，再交给协议适配层处理服务差异。  收益是循环不用同时知道 UI 角色和每家 API 的格式；代价是多一层转换，错误也可能在转换中产生。必要性来自两个独立变化轴，而不是因为“层越多越高级”。一个只接单一 API 的固定流程未必需要同样规模的适配设施。  源码阅读入口：messages.ts 的角色转换、agent-loop.ts 的调用边界、ai/types.ts 的规范化上下文。 "}, {"title": "09 把实现读成设计判断 / 原则二：区分事实、视图和过程", "url": "principles.html#state", "text": " 问题：历史需要完整、模型输入需要有限、UI 又需要实时。方案：会话记录、上下文投影和运行状态分开，使用事件让展示跟进过程。  这个思想类似数据库中的记录与查询视图，但不要把所有 pi 模块都概括为完整的 event sourcing 系统。基础 Agent 仍在内存里维护可变状态，部分事件对象也不是不可变快照。  收益是压缩不必抹去审计历史；代价是恢复和投影规则必须一致。检查设计时应问：哪个对象是权威来源？哪些值能重建？发生失败时谁负责持久保存？ "}, {"title": "09 把实现读成设计判断 / 原则三：把时间顺序写成契约", "url": "principles.html#ordering", "text": " 问题：并发读取 B 比 A 先完成，UI 要尽快更新，对话却需要稳定顺序。方案：执行结束事件按完成时机发布，结果消息按调用源顺序发布。  同样，agent_end 与 idle 分开，取消与 I/O 结算分开。收益是接入者可以明确判断何时开始下一次工作；代价是事件名称本身不足以解释全部行为，必须阅读等待规则。  值得借鉴的是“区分几个看起来相同的完成时刻”。不应机械复制事件数量，也不应以回调很多来证明系统先进。 "}, {"title": "09 把实现读成设计判断 / 原则四：副作用比纯数据更需要保守处理", "url": "principles.html#effects", "text": " 问题：被截断的 JSON 可能还能解析，但缺少关键内容。方案：模型因 length 停止时不执行该响应中的工具调用。问题：取消后的文件写入可能仍在进行。方案：修改队列等实际操作结束后才释放。  这些判断都优先避免不完整输入或未知完成状态造成额外损坏。代价是某些本来可能有效的调用也会被拒绝，需要重新生成。对真实文件操作，这个保守选择通常比“尽量执行”更容易解释。  边界仍然存在：预检不是沙箱，进程内队列不是分布式锁。局部不变量成立，并不意味着整个执行环境已经安全。 "}, {"title": "09 把实现读成设计判断 / 原则五：可扩展，但保持核心责任可见", "url": "principles.html#extension", "text": " 问题：不同用户想换工具、摘要策略和界面。方案：通过函数注入、工具契约与生命周期钩子接入。SDK 是集中装配点，核心循环保留明确流程。  收益是定制通常不需要复制循环。代价是扩展行为会影响状态和时间顺序，宿主需要限制和解释回调的作用范围。当前默认 streamFn 又提示我们：现实代码会同时存在新接口与兼容路径，不能只提炼最理想的一面。  对代码风格的归纳是：普通函数与接口承载多数设计，联合类型表达分支，显式 await 表达顺序，短注释说明不变量与异常情形。并非每个文件都简单；AgentSession 的职责和体量也提示产品演进会累积复杂度。 "}, {"title": "09 把实现读成设计判断 / 如何判断你已经理解这套设计", "url": "principles.html#evaluate", "text": " 试着向同事解释：为什么模型不能自己读文件；为什么工具结果必须回到下一次请求；为什么同一条会话不等于每次发送同一批消息；为什么“取消成功”不能证明文件没有修改；为什么一个新的包不代表它已成为 CLI 的默认路径。   查看参考答案    为什么模型不能自己读文件？  模型只生成数据；文件读取是宿主拥有的副作用。通过工具边界，宿主才能验证参数、限制权限、记录行为并返回真实结果。   为什么工具结果必须回到下一次请求？  工具在模型服务之外执行，模型不会自动知道结果。只有把 toolResult 加入上下文，下一 turn 才能依据外部事实继续推理。   为什么同一条会话不等于每次发送同一批消息？  session 是完整权威记录，模型请求是按当前分支、预算、压缩和过滤规则构建的投影；记录相同也可能产生不同请求视图。   为什么“取消成功”不能证明文件没有修改？  取消是协作信号，不会回滚已经开始或提交的 I/O。必须等待具体工具结算或通过额外事务机制确认副作用状态。   为什么一个新的包不代表它已成为 CLI 的默认路径？  包可能是实验接口、未来方向或独立能力。只有从产品入口追踪到实际调用，并核对公开导出和测试，才能判断当前集成状态。    如果能用具体执行轨迹回答这些问题，你已经获得了迁移到 Go 或 Rust 的设计基础。下一步应根据自己的任务选择需要的边界，而不是把 pi 的目录结构整体翻译一遍。  最终判断：pi 提供了值得研究的边界设计、显式生命周期和可扩展机制，也存在兼容状态、复杂回调与演进中接口。本书把它当作真实工程案例，不给出缺少比较基准的“行业第一”结论。 "}, {"title": "把实现读成设计判断 / 不完整响应中的工具调用不执行。", "url": "principles.html#evidence", "text": "  不完整响应中的工具调用不执行。  packages/agent/src/agent-loop.ts · L233    233  \t\t\t\t// A \"length\" stop means the output was cut off by the token limit, so\n 234  \t\t\t\t// every tool call in the message may carry truncated arguments. Fail\n 235  \t\t\t\t// them all instead of executing potentially borked calls.\n 236  \t\t\t\tconst executedToolBatch =\n 237  \t\t\t\t\tmessage.stopReason === \"length\"\n 238  \t\t\t\t\t\t? await failToolCallsFromTruncatedMessage(toolCalls, emit)\n 239  \t\t\t\t\t\t: await executeToolCalls(currentContext, message, config, signal, emit);\n 240  \t\t\t\ttoolResults.push(...executedToolBatch.messages);\n 241  \t\t\t\thasMoreToolCalls = !executedToolBatch.terminate;\n 242     查看本地源码快照与行号  "}, {"title": "把实现读成设计判断 / 完成事件与空闲状态之间有明确边界。", "url": "principles.html#evidence", "text": "  完成事件与空闲状态之间有明确边界。  packages/agent/src/agent.ts · L262    262  \t * `agent_end` is the final emitted event for a run, but the agent does not\n 263  \t * become idle until all awaited listeners for that event have settled.\n 264  \t */   查看本地源码快照与行号  "}, {"title": "把实现读成设计判断 / 兼容默认值仍真实存在。", "url": "principles.html#evidence", "text": "  兼容默认值仍真实存在。  packages/coding-agent/src/core/sdk.ts · L37     37  setDefaultStreamFn(streamSimple);   查看本地源码快照与行号  "}, {"title": "把实现读成设计判断 / 应用角色转换集中在一个边界。", "url": "principles.html#evidence", "text": "  应用角色转换集中在一个边界。  packages/coding-agent/src/core/messages.ts · L148    148  export function convertToLlm(messages: AgentMessage[]): Message[] {\n 149  \treturn messages\n 150  \t\t.map((m): Message | undefined => {\n 151  \t\t\tswitch (m.role) {\n 152  \t\t\t\tcase \"bashExecution\":\n 153  \t\t\t\t\t// Skip messages excluded from context (!! prefix)\n 154  \t\t\t\t\tif (m.excludeFromContext) {\n 155  \t\t\t\t\t\treturn undefined;\n 156  \t\t\t\t\t}\n 157  \t\t\t\t\treturn {\n 158  \t\t\t\t\t\trole: \"user\",\n 159  \t\t\t\t\t\tcontent: [{ type: \"text\", text: bashExecutionToText(m) }],\n 160  \t\t\t\t\t\ttimestamp: m.timestamp,\n 161  \t\t\t\t\t};\n 162  \t\t\t\tcase \"custom\": {\n 163  \t\t\t\t\tconst content = typeof m.content === \"string\" ? [{ type: \"text\" as const, text: m.content }] : m.content;\n 164  \t\t\t\t\treturn {\n 165  \t\t\t\t\t\trole: \"user\",   查看本地源码快照与行号  "}, {"title": "术语与阅读路线", "url": "reference.html", "text": " 术语与阅读路线  遇到陌生名词时回到这里。先理解角色和时间边界，再追踪具体实现。  术语表   Agent  模型与工具执行构成的反馈系统；在本书中以 pi 的具体运行时为准。  阅读相关章节   AgentSession  coding-agent 的应用协调层，连接 Agent、持久会话、设置、扩展、压缩和重试，并向不同界面提供统一操作。  阅读相关章节   SessionManager  当前 coding-agent 的持久会话管理器，保存带父子关系的消息和状态条目。  阅读相关章节   Harness  围绕模型执行的宿主设施，管理工具、上下文与生命周期；新 Harness 的范围比基础循环更广。  阅读相关章节   Provider / API  服务来源 / 协议实现。二者不是同一个维度。  阅读相关章节   Transcript  按顺序组织的消息记录；其中的 system 更新也承载规则和工具变化。  阅读相关章节   Context  当前操作的上下文；模型上下文与 Chord 的取消/调用上下文是不同概念。  阅读相关章节   Tool call / Tool result  模型提出的结构化请求 / 程序执行后返回的反馈，通过调用 ID 关联。  阅读相关章节   Token / Context window  模型输入输出的计量单位 / 请求可以容纳的上下文容量。  阅读相关章节   Turn / Run  低层 turn 是一次模型响应及其工具批次；一次 run 可有多个 turn。压缩模块的轮次定义不同。  阅读相关章节   Steering / Follow-up  下一轮介入当前工作 / 当前工作本来要结束时追加任务。  阅读相关章节   Compaction  用摘要与近期消息构建更小的模型上下文，不等于删除完整会话。  阅读相关章节   Promise / AsyncIterable  未来完成的结果 / 可按到达顺序异步迭代的数据源。  阅读相关章节   Schema  运行时可以验证的数据结构规则，与擦除后的 TypeScript 类型有别。  阅读相关章节   Facet  插件在某个运行环境中的组成部分，由宿主装配依赖与生命周期。  阅读相关章节   Attachment  某个展示端与会话的一次挂接身份，用于拒绝过期路由。  阅读相关章节   Projection  从记录中构建用于特定目的的视图，例如当前路径的模型上下文。  阅读相关章节   Side effect  改变外部世界的操作，例如写文件；取消或重试需要考虑其真实完成状态。  阅读相关章节    三遍阅读源码    第一遍：数据与边界。 先看 agent/types.ts 的 StreamFn、AgentTool、AgentEvent，再看 ai/types.ts 的消息角色。只回答“数据由谁提供、流向哪里”。   第二遍：执行与结算。 读 agent-loop.ts 的 runLoop、streamAssistantResponse、executeToolCalls，再读 agent.ts 的 processEvents 与 finishRun。把每个 await 前后发生的事情串起来。   第三遍：产品与演进。 读 SDK 装配、messages.ts 转换、compaction 切点与摘要，再读运行时替换。之后才进入 Chord 和新存储方案，并核对公开导出与设计文档的差距。   设计原则索引    变化在边界收敛 ：类型、转换与协议适配。   区分事实、视图与过程 ：会话、上下文、运行状态。   把时间顺序写成契约 ：并发、事件和结算。   保守处理副作用 ：不完整参数、取消与恢复。   可扩展且责任可见 ：注入、工具和生命周期。  "}];
